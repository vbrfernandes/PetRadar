======================================================================
PETRADAR — STATUS DA REFATORAÇÃO DO FRONTEND
============================================

Data de início:
16/08/2026

Objetivo atual:
Refatorar estruturalmente o frontend existente preservando funcionalidades e comportamento. Não implementar novos RFs enquanto o Plano Mestre estiver em execução.

Branch base:
main

Branch de refatoração:
refactor/frontend-modularization

Commit de baseline:
c38a9edeab3b5e3f48047515f150ff397560110f

Mensagem da baseline:
commit Inicial de refatoração de codigo

======================================================================
FASE ATUAL
==========

FASE 7 — OCCURRENCES

Status:
EM ANDAMENTO

Subetapa atual:
FASE 7P — InitialCareSection.

======================================================================
FASE 1 — NAVIGATION — CONCLUÍDA
================================

FASE 1 — NAVIGATION

Status:
CONCLUÍDA

Subetapas:
[X] navigation.types.ts
[X] AuthNavigator.tsx
[X] AppNavigator.tsx
[X] RootNavigator.tsx
[X] decisão autenticado/não autenticado fora do App.tsx
[X] validação final da Fase 1

Tipos extraídos sem alteração de contrato:
* AuthStackParamList
* PetOcorrenciaPrefill
* AppTabParamList

RootNavigator concentra a decisão autenticado/não autenticado.
NavigationContainer e providers permanecem no App.tsx.

Validações da subetapa:
[X] Contratos de navegação preservados integralmente
[X] Nenhuma Screen ou componente importa esses tipos do App.tsx
[X] `npx tsc --noEmit` concluído com 0 erros
[X] `git diff --check` concluído sem erros
[X] Nenhuma rota, parâmetro ou comportamento alterado
[X] Mapbox, backend, dependências e contratos de API não alterados
[X] Rotas, ordem e screenOptions do AuthNavigator preservados integralmente
[X] Rotas, ordem, tabBar e screenOptions do AppNavigator preservados integralmente
[X] SOSScreen e ProfileScreen temporários movidos sem alteração de JSX
[X] Decisão autenticado/não autenticado extraída sem alteração de comportamento
[X] AuthNavigator, AppNavigator e navigation.types.ts não alterados na Fase 1D

Smoke test da Fase 1B — AuthNavigator:
APROVADO MANUALMENTE PELO USUÁRIO.

Smoke test da Fase 1C — AppNavigator:
APROVADO MANUALMENTE PELO USUÁRIO.

Smoke test da Fase 1D — RootNavigator:
APROVADO MANUALMENTE PELO USUÁRIO.

Fechamento formal da Fase 1 — Navigation:
[X] navigation.types.ts extraído
[X] AuthNavigator.tsx extraído
[X] AppNavigator.tsx extraído
[X] RootNavigator.tsx extraído
[X] decisão autenticado/não autenticado removida do App.tsx
[X] contratos de navegação preservados
[X] rotas e parâmetros preservados
[X] smoke tests das Fases 1B, 1C e 1D aprovados manualmente pelo usuário
[X] nenhum RF novo implementado
[X] nenhuma alteração de backend realizada
[X] Mapbox não alterado
[X] nenhuma dependência alterada

======================================================================
FASE 2 — ESTRUTURA MODULAR BASE
================================

Status:
CONCLUÍDA

Checkpoint arquitetural:
[X] domínios atuais identificados
[X] auth definido
[X] occurrences definido
[X] feed definido
[X] map definido
[X] profile definido
[X] pets definido
[X] shared reservado apenas para código realmente genérico
[X] infrastructure reservado para integrações tecnológicas
[X] política de criação progressiva de diretórios definida
[X] nenhuma pasta vazia criada
[X] nenhum arquivo `.gitkeep` criado
[X] nenhum arquivo da aplicação movido
[X] nenhum comportamento alterado
[X] `npx tsc --noEmit` concluído com 0 erros

Estrutura real analisada em `mobile/src/`:
* `components/`: AnimatedCTA.tsx, AppNavigationDrawer.tsx, FeedBannerCarousel.tsx, OccurrenceDetailDrawer.tsx e ProfileQuickMenu.tsx.
* `navigation/`: navigation.types.ts, AuthNavigator.tsx, AppNavigator.tsx e RootNavigator.tsx.
* `screens/`: CadastroOcorrenciaScreen.tsx, CadastroONGScreen.tsx, CadastroUserScreen.tsx, EsqueceuSenhaScreen.tsx, FeedNoticias.tsx, LoginScreen.tsx, MapScreen.tsx, ProfileDetailScreen.tsx, RegistrarPet.tsx e TipoCadastroScreen.tsx.
* `services/`: api.ts.
* `store/`: useAuthStore.ts.
* `theme/`: colors.ts.
* `types/`: diretório vazio preexistente, não alterado nesta fase.

Domínios atuais confirmados:
* `features/auth/`
* `features/occurrences/`
* `features/feed/`
* `features/map/`
* `features/profile/`
* `features/pets/`
* `shared/`, reservado para código realmente genérico após validação definitiva na Fase 11.
* `infrastructure/`, reservado para integrações tecnológicas a partir da Fase 10.

Mapeamento progressivo validado no código real:

AUTH:
* `mobile/src/screens/LoginScreen.tsx` → `features/auth/screens/LoginScreen.tsx`.
* `mobile/src/screens/EsqueceuSenhaScreen.tsx` → `features/auth/screens/EsqueceuSenhaScreen.tsx`.
* `mobile/src/screens/TipoCadastroScreen.tsx` → `features/auth/screens/TipoCadastroScreen.tsx`.
* `mobile/src/screens/CadastroUserScreen.tsx` → `features/auth/screens/CadastroUserScreen.tsx`.
* `mobile/src/screens/CadastroONGScreen.tsx` → `features/auth/screens/CadastroONGScreen.tsx`.
* `mobile/src/store/useAuthStore.ts` pertence conceitualmente ao estado de autenticação e permanece no local atual até decisão específica de migração.
* Os nomes atuais serão preservados na primeira movimentação para reduzir risco.

OCCURRENCES:
* `mobile/src/screens/CadastroOcorrenciaScreen.tsx` → `features/occurrences/screens/CadastroOcorrenciaScreen.tsx`.
* `mobile/src/components/OccurrenceDetailDrawer.tsx` → `features/occurrences/components/OccurrenceDetailDrawer.tsx`.

FEED:
* `mobile/src/screens/FeedNoticias.tsx` → `features/feed/screens/FeedNoticias.tsx`.
* `mobile/src/components/FeedBannerCarousel.tsx` → candidato confirmado a `features/feed/components/FeedBannerCarousel.tsx`.

MAP:
* `mobile/src/screens/MapScreen.tsx` → `features/map/screens/MapScreen.tsx`.

PROFILE:
* `mobile/src/screens/ProfileDetailScreen.tsx` continua implementado como Modal/Drawer e tem destino conceitual `features/profile/components/ProfileDetailDrawer.tsx`; não foi renomeado nem movido nesta fase.
* `mobile/src/components/ProfileQuickMenu.tsx` → candidato confirmado a `features/profile/components/ProfileQuickMenu.tsx`.

PETS:
* `mobile/src/screens/RegistrarPet.tsx` é renderizado dentro de ProfileDetailScreen e não é rota independente; tem destino conceitual `features/pets/components/RegistrarPet.tsx`, sem renomeação ou movimentação nesta fase.

SHARED — candidatos, sem decisão definitiva de movimentação:
* `mobile/src/components/AnimatedCTA.tsx` → candidato a `shared/components/`; atualmente não possui consumidor no código real.
* `mobile/src/components/AppNavigationDrawer.tsx` → candidato a `shared/components/`; atualmente é utilizado por FeedNoticias e MapScreen.
* A classificação definitiva será realizada na Fase 11.

INFRASTRUCTURE — integrações reservadas para migração futura:
* `mobile/src/services/api.ts` permanece no local atual; eventual migração para `infrastructure/api/` ocorrerá somente na Fase 10.
* Acesso a `expo-location` fica reservado conceitualmente para `infrastructure/location/`.
* Configuração/acesso ao Mapbox fica reservado conceitualmente para `infrastructure/maps/`, sem substituir Mapbox.
* Acesso a `expo-image-picker` fica reservado conceitualmente para `infrastructure/media/`.
* Persistência tecnológica baseada em AsyncStorage fica reservada conceitualmente para `infrastructure/storage/`.

NAVIGATION:
* `mobile/src/navigation/navigation.types.ts`, AuthNavigator.tsx, AppNavigator.tsx e RootNavigator.tsx permanecem onde estão e não foram alterados.

THEME:
* `mobile/src/theme/colors.ts` permanece onde está; sua evolução pertence à Fase 12.

Decisão arquitetural da Fase 2:
“As pastas da arquitetura modular serão criadas progressivamente quando receberem código real. Não serão utilizados diretórios vazios ou .gitkeep apenas para materializar antecipadamente a árvore-alvo.”

Não foram criados os domínios futuros care, engagement, reports, sightings, ong, chat, notifications ou sos. Sua existência futura dependerá de código real suficiente para justificá-los.

======================================================================
FASE 3 — AUTH
=============

Status:
CONCLUÍDA

Subetapas:
[X] TipoCadastroScreen migrada
[X] EsqueceuSenhaScreen migrada
[X] LoginScreen migrada
[X] CadastroUserScreen migrada
[X] CadastroONGScreen migrada
[X] necessidade de authService confirmada no código real
[X] authService validado manualmente
[X] organização interna de Auth concluída
[X] validação final da Fase 3

Subetapa Fase 3A — TipoCadastroScreen:
* Movido `mobile/src/screens/TipoCadastroScreen.tsx` para `mobile/src/features/auth/screens/TipoCadastroScreen.tsx`.
* Primeiro diretório físico de `features/` criado com código real.
* Nenhuma pasta vazia ou arquivo `.gitkeep` criado.
* Nome TipoCadastroScreen preservado.
* JSX, estilos, textos, navegação, handlers, cores, accessibility e comportamento preservados.
* O `any` preexistente de `navigation` foi preservado por estar fora do escopo desta movimentação.
* AuthNavigator foi o único consumidor executável encontrado e teve somente o caminho do import atualizado.
* As rotas, sua ordem, screenOptions e AuthStackParamList não foram alterados.
* LoginScreen.tsx, EsqueceuSenhaScreen.tsx, CadastroUserScreen.tsx e CadastroONGScreen.tsx permanecem em `mobile/src/screens/`.
* useAuthStore.ts não foi movido.

Smoke test Fase 3A:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login abre.
* Escolha de tipo de cadastro abre.
* TipoCadastroScreen aparece normalmente.
* Botão voltar funciona.
* Opção CadastroUser continua abrindo.
* Opção CadastroONG continua abrindo.
* Opção Fazer Login continua funcionando.

Subetapa Fase 3B — EsqueceuSenhaScreen:
* Movido `mobile/src/screens/EsqueceuSenhaScreen.tsx` para `mobile/src/features/auth/screens/EsqueceuSenhaScreen.tsx`.
* Nome EsqueceuSenhaScreen preservado.
* JSX, estados, fluxo de etapas, handlers, validações, Alerts, textos, estilos, cores, loading, navegação, accessibility e comportamento preservados.
* Import relativo de API ajustado de `../services/api` para `../../../services/api` exclusivamente por necessidade física da movimentação.
* O novo import continua resolvendo para o mesmo módulo real `mobile/src/services/api.ts`.
* Endpoints `/auth/esqueceu-senha` e `/auth/redefinir-senha` e contratos de API não foram alterados.
* O `navigation: any` preexistente foi preservado por estar fora do escopo.
* AuthNavigator teve somente o caminho do import de EsqueceuSenhaScreen atualizado.
* Nome da rota EsqueceuSenha, ordem das rotas, screenOptions e AuthStackParamList não foram alterados.
* TipoCadastroScreen permaneceu intacta.
* LoginScreen.tsx, CadastroUserScreen.tsx e CadastroONGScreen.tsx permanecem em `mobile/src/screens/`.
* services/api.ts e useAuthStore.ts não foram movidos ou alterados.
* Nenhuma pasta vazia ou arquivo `.gitkeep` criado.

Smoke test Fase 3B:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login abre.
* A ação “Esqueci minha senha” abre EsqueceuSenhaScreen.
* EsqueceuSenhaScreen abre normalmente.
* Voltar funciona.
* Envio e validação inicial continuam se comportando como antes.
* Navegação não apresenta erro.

Subetapa Fase 3C — LoginScreen:
* Movido `mobile/src/screens/LoginScreen.tsx` para `mobile/src/features/auth/screens/LoginScreen.tsx`.
* Nome LoginScreen preservado.
* JSX, estados, email, senha, showPassword, loading, handleLogin, validações, Alerts, chamada HTTP, setAuth, textos, estilos, cores, imagem, navegação, accessibility e comportamento preservados.
* Import relativo de API ajustado de `../services/api` para `../../../services/api` exclusivamente por necessidade física da movimentação.
* Import relativo da store ajustado de `../store/useAuthStore` para `../../../store/useAuthStore` exclusivamente por necessidade física da movimentação.
* O caminho relativo da imagem foi ajustado de `../../assets/logo/logo.png` para `../../../../assets/logo/logo.png` exclusivamente por necessidade física da movimentação.
* Os novos caminhos continuam resolvendo para os mesmos arquivos reais `mobile/src/services/api.ts`, `mobile/src/store/useAuthStore.ts` e `mobile/assets/logo/logo.png`.
* Endpoint `/auth/login`, payload com `email` e `senha`, leitura de `access_token` e `user` e chamada de `setAuth` não foram alterados.
* O `navigation: any` preexistente foi preservado por estar fora do escopo.
* AuthNavigator teve somente o caminho do import de LoginScreen atualizado.
* Nomes das rotas Login, TipoCadastro e EsqueceuSenha, ordem das rotas, screenOptions e AuthStackParamList não foram alterados.
* TipoCadastroScreen e EsqueceuSenhaScreen permaneceram intactas.
* CadastroUserScreen.tsx e CadastroONGScreen.tsx permanecem em `mobile/src/screens/`.
* services/api.ts e store/useAuthStore.ts não foram movidos ou alterados.
* Nenhuma pasta vazia ou arquivo `.gitkeep` criado.

Smoke test Fase 3C:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Aplicativo abre na tela Login quando deslogado.
* LoginScreen aparece normalmente.
* Validação de campos vazios funciona.
* Botão mostrar/ocultar senha funciona.
* “Esqueceu a senha?” abre EsqueceuSenhaScreen.
* Voltar funciona.
* “Cadastre-se” abre TipoCadastroScreen.
* Login real continua funcionando.
* Após login, o aplicativo principal abre.
* Nenhum erro ou crash ocorre.

Subetapa Fase 3D — CadastroUserScreen:
* Movido `mobile/src/screens/CadastroUserScreen.tsx` para `mobile/src/features/auth/screens/CadastroUserScreen.tsx`.
* Nome do arquivo CadastroUserScreen.tsx preservado.
* Nome interno exportado CadastroUsuarioScreen preservado.
* JSX, formulário, etapas, estados, validações, nome, CPF, telefone, possui pet, email, senha, confirmação de senha, localização, loading, Alerts, payload, tratamento de erro, navegação, textos, estilos, cores, accessibility e comportamento preservados.
* Import relativo de API ajustado de `../services/api` para `../../../services/api` exclusivamente por necessidade física da movimentação.
* O novo import continua resolvendo para o mesmo módulo real `mobile/src/services/api.ts`.
* Import direto de `expo-location` preservado.
* `Location.requestForegroundPermissionsAsync()`, `Location.getCurrentPositionAsync()` e `Location.Accuracy.Balanced` preservados sem alteração de permissão, precisão ou fallback.
* Endpoint `/auth/registro/usuario` e payload com `nome_completo`, `cpf`, `telefone`, `tem_pet`, `email`, `senha`, `localizacao_lat` e `localizacao_lng` não foram alterados.
* Navegação para a rota Login após sucesso preservada.
* Os `navigation: any` e `catch (error: any)` preexistentes foram preservados por estarem fora do escopo.
* AuthNavigator teve somente o caminho do import de CadastroUserScreen atualizado.
* Nomes das rotas CadastroUser e Login, ordem das rotas, screenOptions e AuthStackParamList não foram alterados.
* LoginScreen, TipoCadastroScreen e EsqueceuSenhaScreen permaneceram intactas.
* CadastroONGScreen.tsx permanece em `mobile/src/screens/`.
* services/api.ts não foi movido ou alterado.
* Nenhuma pasta vazia ou arquivo `.gitkeep` criado.

Smoke test Fase 3D:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login abre.
* “Cadastre-se” abre TipoCadastroScreen.
* A opção de cadastro de usuário abre CadastroUserScreen.
* CadastroUserScreen aparece normalmente.
* Avançar e voltar entre etapas funciona.
* Validações obrigatórias continuam funcionando.
* Switch de possui pet continua funcionando.
* Voltar funciona.
* Nenhum erro ou crash ocorre.

Subetapa Fase 3E — CadastroONGScreen:
* Movido `mobile/src/screens/CadastroONGScreen.tsx` para `mobile/src/features/auth/screens/CadastroONGScreen.tsx`.
* Última tela exclusivamente Auth movida para `mobile/src/features/auth/screens/`; nenhuma tela Auth permanece em `mobile/src/screens/`.
* Nome do arquivo e do componente CadastroONGScreen preservados.
* JSX, formulário, fluxo de três etapas, estados, validações, dados institucionais, capacidade, gestor, acesso, localização, comprovante, loading, Alerts, payload, tratamento de erro, navegação, textos, estilos, cores, accessibility e comportamento preservados.
* Import relativo de API ajustado de `../services/api` para `../../../services/api` exclusivamente por necessidade física da movimentação.
* O novo import continua resolvendo para o mesmo módulo real `mobile/src/services/api.ts`.
* Imports diretos de `expo-location` e `expo-image-picker` preservados.
* `Location.requestForegroundPermissionsAsync()`, `Location.getCurrentPositionAsync()` e `Location.Accuracy.Balanced` preservados sem alteração de permissão, precisão, localização opcional ou fallback nulo.
* `ImagePicker.launchImageLibraryAsync()` preservado com `ImagePicker.MediaTypeOptions.Images`, `allowsEditing: true` e `quality: 0.8`.
* `comprovanteUri`, `setComprovanteUri` e `selecionarComprovante` preservados conforme o comportamento atual; nenhum upload ou campo de payload foi adicionado.
* Endpoint `/auth/registro/ong` e payload real não foram alterados.
* Conversões `parseInt`, chamadas `trim`, valores nulos e fallbacks foram preservados.
* Navegação para a rota Login após sucesso preservada.
* Os `navigation: any` e `catch (error: any)` preexistentes foram preservados por estarem fora do escopo.
* AuthNavigator teve somente o caminho do import de CadastroONGScreen atualizado; todas as cinco telas agora são importadas de `features/auth/screens/`.
* Nomes e ordem das rotas, screenOptions e AuthStackParamList não foram alterados.
* LoginScreen, TipoCadastroScreen, EsqueceuSenhaScreen e CadastroUserScreen permaneceram intactas.
* services/api.ts não foi movido ou alterado.
* Nenhuma organização interna de Auth foi realizada.
* Nenhuma pasta vazia ou arquivo `.gitkeep` criado.

Smoke test Fase 3E:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login abre.
* “Cadastre-se” abre TipoCadastroScreen.
* A opção CadastroONG abre CadastroONGScreen.
* CadastroONGScreen aparece normalmente.
* Avançar e voltar entre as três etapas funciona.
* Validações obrigatórias continuam funcionando.
* Switches continuam funcionando.
* Botão de selecionar comprovante abre o seletor normalmente.
* Cancelar o seletor não causa erro.
* Voltar funciona.
* Nenhum erro ou crash ocorre.

Subetapa Fase 3F — authService:
* Criado `mobile/src/features/auth/services/authService.ts` com código real para as cinco operações HTTP existentes do domínio Auth.
* A criação do serviço foi justificada no código real por quatro Screens que acessavam diretamente o cliente HTTP para cinco operações do domínio.
* LoginScreen, EsqueceuSenhaScreen, CadastroUserScreen e CadastroONGScreen deixaram de importar diretamente `mobile/src/services/api.ts`.
* O authService utiliza o cliente HTTP existente por meio do caminho relativo `../../../services/api`; api.ts, baseURL, timeout, headers e interceptors não foram alterados.
* Endpoints `/auth/login`, `/auth/esqueceu-senha`, `/auth/redefinir-senha`, `/auth/registro/usuario` e `/auth/registro/ong` foram centralizados sem alteração.
* Payloads específicos foram tipados no próprio authService, sem `any`, sem `Record<string, any>` e sem criar auth.types.ts sem reuso concreto.
* As Screens continuam responsáveis pela preparação dos payloads, trim, toLowerCase, parseInt, fallbacks nulos, validações, Alerts, loading, navegação e estado visual.
* LoginScreen continua lendo `access_token` e `user` da resposta e chamando `setAuth` diretamente; o serviço não conhece Zustand.
* expo-location permaneceu diretamente nos cadastros de usuário e ONG, sem alteração de permissão, precisão ou fallback.
* expo-image-picker, comprovanteUri e o fluxo de seleção de comprovante permaneceram na CadastroONGScreen; o comprovante não foi adicionado ao payload.
* TipoCadastroScreen, AuthNavigator, useAuthStore.ts, rotas, nomes de Screens, backend, dependências e contratos de API não foram alterados.
* Hooks de Auth e renomeações de Screens não foram introduzidos nesta subetapa.
* `npx tsc --noEmit` concluído com 0 erros.

Smoke test Fase 3F:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login abre e suas validações continuam funcionando.
* Login real funciona e abre a aplicação após autenticação.
* Logout retorna para Login normalmente.
* “Esqueceu a senha?” abre e as validações de recuperação continuam funcionando.
* Cadastro de usuário abre e permite navegar entre as etapas.
* Cadastro de ONG abre e permite navegar entre as etapas.
* Nenhum erro ou crash ocorre.

Subetapa Fase 3G — fechamento formal da Fase 3 — Auth:

Decisão final de organização interna:
* authService.ts é mantido porque possui uso real e centraliza cinco operações HTTP do domínio Auth.
* auth.types.ts não foi criado porque os tipos atuais possuem uso específico dentro de authService.ts e não existe reuso concreto suficiente para justificar arquivo separado.
* useLogin.ts, useRegister.ts e outros hooks Auth não foram criados porque a lógica restante é específica das Screens e seu deslocamento não produziria benefício arquitetural comprovado.
* useAuthStore.ts permanece em `mobile/src/store/useAuthStore.ts` porque possui consumidores transversais e sua movimentação não pertence ao escopo desta fase.
* services/api.ts permanece em `mobile/src/services/api.ts`; sua eventual organização tecnológica continua reservada para a Fase 10 — Infrastructure.
* Renomeações de arquivos e componentes Auth permanecem adiadas.
* Nenhuma alteração adicional de código foi justificada ou realizada no fechamento formal.

Fechamento formal da Fase 3 — Auth:
[X] cinco Screens Auth residem em features/auth/screens/
[X] authService reside em features/auth/services/
[X] cinco endpoints Auth centralizados
[X] Screens não acessam diretamente api.ts para operações Auth
[X] contratos HTTP preservados
[X] payloads preservados
[X] navegação preservada
[X] AuthNavigator preservado
[X] useAuthStore preservado
[X] expo-location preservado
[X] expo-image-picker preservado
[X] nenhuma dependência alterada
[X] nenhum backend alterado
[X] nenhum RF novo implementado
[X] TypeScript com 0 erros
[X] smoke tests 3A, 3B, 3C, 3D, 3E e 3F aprovados
[X] nenhuma abstração sem uso real criada
[X] nenhuma pasta vazia criada
[X] nenhum .gitkeep criado
[X] organização interna de Auth concluída
[X] Fase 3 concluída

Validações técnicas finais da Fase 3:
* `npx tsc --noEmit` concluído com 0 erros.
* Os cinco endpoints Auth aparecem no código executável somente em `mobile/src/features/auth/services/authService.ts`.
* Nenhuma Screen em `mobile/src/features/auth/screens/` importa diretamente services/api.
* `git diff --check` global retornou exit code 2 exclusivamente pelo trailing whitespace preexistente em codigo_frontend_consolidado.txt e codigo_projeto_consolidado.txt; essa condição não representa falha da Fase 3.

======================================================================
FASE 4 — FEED
=============

Status:
CONCLUÍDA

Subetapas:
[X] FeedBannerCarousel migrado
[X] FeedNoticias migrada
[X] organização interna do Feed avaliada
[X] validação final da Fase 4

Subetapa Fase 4A — FeedBannerCarousel:
* Movido `mobile/src/components/FeedBannerCarousel.tsx` para `mobile/src/features/feed/components/FeedBannerCarousel.tsx`.
* O domínio físico `mobile/src/features/feed/` foi criado somente ao receber código real.
* `mobile/src/features/feed/components/` é o único diretório criado para Feed nesta subetapa e contém somente FeedBannerCarousel.tsx.
* Nome do arquivo e do componente FeedBannerCarousel preservados.
* Comentário documental de caminho atualizado para o novo local físico.
* Import relativo de theme ajustado de `../theme/colors` para `../../../theme/colors`, continuando a resolver para o mesmo módulo `mobile/src/theme/colors.ts`.
* FeedNoticias foi o único consumidor executável encontrado e teve somente o caminho do import de FeedBannerCarousel atualizado.
* FeedNoticias permanece em `mobile/src/screens/FeedNoticias.tsx`.
* BANNERS, ids, tipos APP/SPONSORED, títulos, descrições e ícones foram preservados integralmente.
* AUTO_PLAY_INTERVAL_MS permanece em 6000 ms.
* React hooks, FlatList, dimensões, autoplay, activeIndex, refs, scrollToIndex, swipe manual, paginação, labels, JSX, estilos e accessibility foram preservados.
* Comparação normalizada do componente antes/depois, desconsiderando somente o comentário de caminho e o import relativo do theme, confirmou conteúdo idêntico.
* AppNavigationDrawer.tsx, ProfileQuickMenu.tsx e OccurrenceDetailDrawer.tsx não foram movidos ou alterados.
* Nenhum endpoint, contrato de API, backend, dependência, comportamento visual ou RF foi alterado.
* Nenhuma pasta vazia, index.ts, .gitkeep, Screen, hook, service, types ou abstração adicional de Feed foi criada.
* `npx tsc --noEmit` concluído com 0 erros.

Smoke test Fase 4A:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login funciona.
* Feed abre.
* Carrossel de banners e banner inicial aparecem corretamente.
* Swipe entre banners funciona.
* Indicadores acompanham o banner atual.
* Autoplay continua trocando os banners.
* Interação manual não causa comportamento estranho.
* Restante do Feed continua aparecendo.
* Nenhum erro ou crash ocorre.

Subetapa Fase 4B — FeedNoticias:
* Movido `mobile/src/screens/FeedNoticias.tsx` para `mobile/src/features/feed/screens/FeedNoticias.tsx`.
* `mobile/src/features/feed/screens/` foi criado somente ao receber o código real de FeedNoticias.tsx.
* Nome do arquivo, nome do componente e export default de FeedNoticias preservados.
* AppNavigator foi o único consumidor executável encontrado e teve somente o caminho do import atualizado de `../screens/FeedNoticias` para `../features/feed/screens/FeedNoticias`.
* Rota Feed, componente registrado, ordem das rotas, tabBar, screenOptions e AppTabParamList não foram alterados.
* Imports relativos de navigation.types.ts, api.ts, OccurrenceDetailDrawer.tsx, AppNavigationDrawer.tsx, ProfileQuickMenu.tsx, FeedBannerCarousel.tsx, ProfileDetailScreen.tsx, theme/colors.ts e useAuthStore.ts foram ajustados exclusivamente por necessidade física da movimentação.
* Todos os imports ajustados continuam resolvendo para os mesmos módulos reais anteriores.
* As duas referências ao asset ECO foram ajustadas de `../../assets/ChatGPT Image 15 de ago. de 2026, 11_30_55.png` para `../../../../assets/ChatGPT Image 15 de ago. de 2026, 11_30_55.png` e continuam resolvendo para o mesmo arquivo físico `mobile/assets/ChatGPT Image 15 de ago. de 2026, 11_30_55.png`.
* FeedBannerCarousel permanece intacto em `mobile/src/features/feed/components/FeedBannerCarousel.tsx` e continua sendo consumido diretamente por FeedNoticias.
* ProfileDetailScreen.tsx, ProfileQuickMenu.tsx, OccurrenceDetailDrawer.tsx e AppNavigationDrawer.tsx não foram movidos ou alterados.
* services/api.ts, store/useAuthStore.ts e theme/colors.ts não foram movidos ou alterados.
* Endpoints `/auth/me`, `/ocorrencias/proximas`, `/ocorrencias/${occurrenceId}/forca` e `/ocorrencias/${denunciaOccurrenceId}/denuncias` foram preservados.
* Tipos locais OcorrenciaFeed, ForcaResponse, RecarregarListaOcorrencias, FiltroFeed, ModoFeed, FiltroConfig, StatusVisual e OccurrenceCardProps foram preservados no mesmo arquivo.
* JSX, estados, hooks, handlers, navegação, estilos, textos, accessibility e comportamento foram preservados.
* Comparação normalizada da tela antes/depois, desconsiderando somente comentário de caminho, imports relativos e as duas referências relativas ao asset ECO, confirmou conteúdo idêntico.
* Nenhum feedService, hook, types, componente adicional, index.ts, .gitkeep ou abstração interna de Feed foi criado.
* A organização interna do Feed permanece adiada até a aprovação manual desta movimentação.
* `npx tsc --noEmit` concluído com 0 erros.

Smoke test Fase 4B:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login funciona.
* Feed abre.
* Header aparece normalmente.
* Carrossel de banners continua funcionando.
* Busca continua funcionando.
* Alternância PROXIMIDADE/ECO continua funcionando.
* Swipe do seletor de modo continua funcionando.
* Filtros continuam funcionando.
* Refresh continua funcionando.
* Cards continuam aparecendo.
* Abrir detalhes de ocorrência continua funcionando.
* ECO/força continua funcionando.
* Menu principal continua abrindo.
* Perfil rápido continua abrindo.
* Registrar ocorrência continua navegando.
* Ícones ECO continuam aparecendo a partir do mesmo asset físico.
* Nenhum erro ou crash ocorre.

Subetapa Fase 4C — OccurrenceCard:
* O componente interno OccurrenceCard foi extraído de `mobile/src/features/feed/screens/FeedNoticias.tsx` para `mobile/src/features/feed/components/OccurrenceCard.tsx`.
* A fronteira preexistente e as props occurrence, forcaLoading, onPress, onToggleForca e onOpenOptions foram preservadas.
* JSX, labels, accessibility, status, urgência, autor, avatar, foto, fallback, ações desabilitadas, ECO, contadores, descrição, tempo, distância e comportamento visual foram preservados.
* A animação ECO foi preservada integralmente, incluindo echoRotation, echoScale, previousEchoState, useEffect, interpolate, Animated.parallel, Animated.sequence, durations, Easing e useNativeDriver.
* O card continua usando o mesmo asset físico `mobile/assets/ChatGPT Image 15 de ago. de 2026, 11_30_55.png` pelo caminho relativo `../../../../assets/ChatGPT Image 15 de ago. de 2026, 11_30_55.png`.
* StatusVisual e OccurrenceCardProps passaram a ser internos de OccurrenceCard.tsx e não foram exportados.
* Os helpers capitalizar, formatarDistancia, formatarTempoRelativo, obterStatusVisual, obterTituloOcorrencia, obterDescricaoOcorrencia, obterIniciais e formatarQuantidadeComentarios foram confirmados como exclusivos do card e movidos sem alteração de implementação.
* ehUrgente possuía dois consumidores reais — o card e o filtro URGENTES da Screen — e foi mantido sem duplicação em `mobile/src/features/feed/utils/feed.utils.ts`.
* Criado `mobile/src/features/feed/types/feed.types.ts` somente com a interface OcorrenciaFeed, preservando exatamente seus campos e tipagens; a interface passou a ser compartilhada por FeedNoticias e OccurrenceCard.
* Criado `mobile/src/features/feed/utils/feed.utils.ts` com normalizarTexto preservado exatamente e compartilhado por FeedNoticias e OccurrenceCard; ehUrgente também reside nesse arquivo por possuir os mesmos dois consumidores reais.
* Quarenta styles usados exclusivamente pelo card foram movidos com os mesmos valores para OccurrenceCard.tsx.
* buttonPressed possuía uso compartilhado pelo card e pela Screen; permaneceu como uma única definição em OccurrenceCard.tsx e é reutilizado pela Screen por export nomeado, sem duplicação e sem alterar as props do card.
* FeedNoticias permanece responsável por estado, carregamento, busca, filtros, modos PROXIMIDADE/ECO, operação HTTP de ECO, denúncia, drawers e navegação.
* Todas as chamadas HTTP permaneceram em FeedNoticias; nenhum endpoint, payload, parâmetro ou contrato foi alterado.
* FeedBannerCarousel.tsx e AppNavigator.tsx permaneceram intactos.
* ProfileDetailScreen.tsx, ProfileQuickMenu.tsx, OccurrenceDetailDrawer.tsx e AppNavigationDrawer.tsx não foram movidos ou alterados.
* Nenhum service, hook, outro componente visual, index.ts ou .gitkeep foi criado.
* Comparações normalizadas dos tipos, helpers, corpo do OccurrenceCard e styles confirmaram equivalência com os blocos originais.
* `npx tsc --noEmit` concluído com 0 erros.

Smoke test Fase 4C:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login funciona.
* Feed abre.
* Cards aparecem normalmente.
* Foto e fallback do card funcionam quando aplicáveis.
* Status e badge urgente aparecem corretamente.
* Autor e avatar aparecem normalmente.
* Tocar no card abre os detalhes.
* Menu de três pontos abre as opções.
* ECO e sua animação continuam funcionando.
* Contadores de ECO e comentários continuam atualizando e aparecendo.
* Tempo e distância continuam aparecendo.
* Ícone ECO continua apontando para o mesmo asset.
* Busca, filtros, PROXIMIDADE/ECO e refresh continuam funcionando.
* Nenhum erro ou crash ocorre.

Subetapa Fase 4D — FeedControls:
* Criado `mobile/src/features/feed/components/FeedControls.tsx` como componente visual/presentacional da busca, carrossel de busca/modos, seleção PROXIMIDADE/ECO e filtros do Feed.
* FeedNoticias permanece responsável pelos estados search, modoFeed, filtro, raioPesquisaKm e refreshing, pela lista ocorrenciasFiltradas e por todos os comportamentos associados.
* FeedControls recebe somente search, modoFeed, filtro, raioPesquisaKm, refreshing, quantidadeOcorrenciasFiltradas e os callbacks onSearchChange, onSelectMode e onFilterChange.
* A lógica de ocorrenciasFiltradas permaneceu integralmente em FeedNoticias; normalizarTexto e ehUrgente permaneceram em `mobile/src/features/feed/utils/feed.utils.ts`.
* A pesquisa de consumidores confirmou que feedModeCarouselRef possuía somente a declaração e o uso como ref do ScrollView do carrossel; o ref foi movido para dentro de FeedControls com o mesmo tipo `ScrollView | null` e o mesmo comportamento.
* Os unions literais FiltroFeed e ModoFeed foram movidos sem alteração para `mobile/src/features/feed/types/feed.types.ts` após passarem a possuir dois consumidores reais: FeedNoticias e FeedControls.
* FiltroConfig e FILTROS permanecem internos de FeedControls por possuírem somente esse consumidor real.
* FEED_MODE_CAROUSEL_WIDTH foi movido para FeedControls por ser usado exclusivamente no bloco extraído; Dimensions e TextInput deixaram de ser imports da Screen.
* Foram movidos exclusivamente os 27 styles do bloco: feedModeCarouselWrapper, feedModeCarouselPage, searchBox, searchInput, feedSectionHeader, feedSectionText, feedSectionTitle, feedSectionSubtitle, counterBadge, counterText, feedModeButtonsPage, feedModeButton, feedModeButtonActive, feedModeButtonPressed, feedModeIconBox, feedModeIconBoxActive, feedModeEcoIcon, feedModeContent, feedModeTitle, feedModeTitleActive, feedModeSubtitle, feedModeSubtitleActive, filtersContent, filterChip, filterChipActive, filterText e filterTextActive.
* A comparação normalizada confirmou que os 27 styles foram preservados literalmente, sem alteração de valores ou duplicação.
* A comparação normalizada do JSX transferido, substituindo apenas os acessos diretos da Screen pelas props correspondentes, confirmou conteúdo equivalente.
* O botão ECO continua apontando para o mesmo asset físico `mobile/assets/ChatGPT Image 15 de ago. de 2026, 11_30_55.png`, com o mesmo caminho relativo usado pelos componentes do diretório.
* FeedControls não realiza chamadas HTTP, não acessa api.ts, não solicita localização, não carrega ocorrências, não altera regras de filtragem e não conhece navegação, drawers, perfil ou denúncia.
* GET `/auth/me`, GET `/ocorrencias/proximas`, POST `/ocorrencias/${occurrenceId}/forca` e POST `/ocorrencias/${denunciaOccurrenceId}/denuncias` permaneceram em FeedNoticias sem alteração.
* carregarFeed, alternarForca, enviarDenuncia, refresh, localização, raio, navegação, perfil, drawers, modal de denúncia, ocorrência selecionada, lista de ocorrências e ocorrenciasFiltradas permaneceram na Screen.
* FeedBannerCarousel.tsx e OccurrenceCard.tsx não foram alterados nesta subetapa; seus comportamentos permanecem preservados.
* Nenhum hook, feedService, feature futura, modal de denúncia ou abstração adicional foi criado.
* Arquivo criado: `mobile/src/features/feed/components/FeedControls.tsx`.
* Arquivos de aplicação modificados: `mobile/src/features/feed/screens/FeedNoticias.tsx` e `mobile/src/features/feed/types/feed.types.ts`.
* Arquivo documental modificado: `STATUS_REFATORACAO_PETRADAR.txt`.
* `npx tsc --noEmit` concluído com 0 erros.
* `git diff --check` global retornou exit code 2 exclusivamente pelos 82 trailing whitespaces preexistentes em codigo_frontend_consolidado.txt e codigo_projeto_consolidado.txt; nenhum erro foi encontrado nos arquivos da Fase 4D.
* Ambiguidade preservada: feedButtonPressedStyle continua definido e exportado por OccurrenceCard porque possui consumidores reais no card e em várias áreas da Screen. FeedControls reutiliza a mesma definição no filtro, sem duplicação. A propriedade arquitetural desse style será reavaliada em etapa futura, sem ampliar o escopo da Fase 4D.

Smoke test Fase 4D:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login funciona.
* Feed abre sem erro ou crash.
* Busca aceita texto, filtra os cards e pode ser limpa pelo ícone.
* O contador de ocorrências acompanha o resultado filtrado.
* O swipe entre a página de busca e a página de modos continua funcionando.
* PROXIMIDADE continua selecionável e exibe o raio correto.
* ECO continua selecionável, exibe o mesmo ícone e carrega o modo correspondente.
* Os botões de modo permanecem desabilitados durante refreshing.
* Todos os filtros aparecem, podem ser selecionados e preservam o resultado esperado.
* Refresh continua funcionando.
* Cards, detalhes, ECO/força, menus e navegação continuam funcionando.
* FeedBannerCarousel e OccurrenceCard continuam visualmente e funcionalmente iguais.

Subetapa Fase 4E — style compartilhado do Feed:
* A pesquisa no código real confirmou três consumidores de feedButtonPressedStyle dentro do domínio Feed: OccurrenceCard, FeedControls e FeedNoticias.
* Criado `mobile/src/features/feed/styles/feed.styles.ts` somente com a definição compartilhada de feedButtonPressedStyle.
* A definição foi movida literalmente de OccurrenceCard para o novo módulo, preservando `opacity: 0.78`.
* OccurrenceCard deixou de exportar feedButtonPressedStyle e passou a importá-lo de `../styles/feed.styles` para seu uso interno.
* FeedControls deixou de importar o style de OccurrenceCard e passou a importá-lo de `../styles/feed.styles`.
* FeedNoticias deixou de importar o style de OccurrenceCard e passou a importá-lo de `../styles/feed.styles`.
* Ao final existe uma única definição de feedButtonPressedStyle e todos os três consumidores reais do domínio Feed utilizam essa definição.
* A pesquisa global também encontrou `styles.buttonPressed` em RegistrarPet, mas esse style pertence ao domínio Pets e possui valores diferentes (`opacity: 0.85` e `scale: 0.99`); portanto não é consumidor nem duplicação do style compartilhado do Feed.
* Todos os demais styles, JSX, handlers, props, accessibility, animações, asset ECO, filtros, busca, modos PROXIMIDADE/ECO, HTTP, endpoints e navegação foram preservados.
* feed.types.ts e feed.utils.ts não foram alterados nesta subetapa.
* Nenhum componente, hook, service ou abstração adicional foi criado.
* Arquivo criado: `mobile/src/features/feed/styles/feed.styles.ts`.
* Arquivos de aplicação modificados nesta subetapa: `mobile/src/features/feed/components/OccurrenceCard.tsx`, `mobile/src/features/feed/components/FeedControls.tsx` e `mobile/src/features/feed/screens/FeedNoticias.tsx`.
* Arquivo documental modificado: `STATUS_REFATORACAO_PETRADAR.txt`.
* `npx tsc --noEmit` concluído com 0 erros.
* `git diff --check` global retornou exit code 2 exclusivamente pelos 82 trailing whitespaces preexistentes em codigo_frontend_consolidado.txt e codigo_projeto_consolidado.txt; a verificação restrita aos arquivos da Fase 4E concluiu sem erros.

Avaliação final da organização interna do Feed:
* carregarFeed permanece em FeedNoticias porque ainda coordena perfil, raio de pesquisa, permissão/localização atual e carregamento de ocorrências.
* ECO/força permanece em FeedNoticias e poderá ser tratado na futura Fase 9 — Care / Engagement / Reports quando houver justificativa real.
* A denúncia, incluindo DENUNCIA_MOTIVOS, estado, envio e modal, permanece em FeedNoticias e poderá ser tratada na futura fase Reports quando houver justificativa real.
* ProfileQuickMenu e ProfileDetailScreen pertencem conceitualmente ao domínio Profile e sua movimentação permanece reservada à Fase 5.
* OccurrenceDetailDrawer pertence conceitualmente ao domínio Occurrences e sua movimentação permanece reservada à Fase 7.
* As integrações diretas de localização e API permanecem para as fases posteriores previstas no Plano Mestre.
* O Header, o estado vazio e o botão Registrar ocorrência permanecem específicos da Screen e não justificam componentes adicionais neste momento.
* A análise do código real não justificou FeedHeader, FeedEmptyState, hooks, feedService ou qualquer outra extração interna nesta subetapa.
* Com a aprovação manual da Fase 4E, não há outra extração interna de Feed atualmente justificada; resta somente o fechamento formal da Fase 4, sem iniciar Profile automaticamente.

Smoke test Fase 4E:
APROVADO MANUALMENTE PELO USUÁRIO.

Validação manual aprovada contemplou:
* Login funciona.
* Feed abre sem erro ou crash.
* Pressionar cards e botões mantém o mesmo feedback visual de opacidade.
* Busca e limpeza da busca continuam funcionando.
* PROXIMIDADE e ECO continuam selecionáveis.
* Filtros continuam funcionando e mantendo o mesmo feedback visual ao pressionar.
* ECO/força e sua animação continuam funcionando.
* Abrir detalhes de ocorrência continua funcionando.
* Menu principal e perfil continuam abrindo.
* Modal de denúncia continua abrindo, cancelando e enviando normalmente.
* Refresh e botão Registrar ocorrência continuam funcionando.

Subetapa Fase 4F — consolidação técnica de 4D + 4E:
* O smoke test manual da Fase 4D permanece aprovado pelo usuário.
* O smoke test manual da Fase 4E foi aprovado pelo usuário.
* FeedControls foi validado manualmente no estado atual do working tree.
* feedButtonPressedStyle compartilhado foi validado manualmente no estado atual do working tree.
* As implementações das Fases 4D e 4E coexistiam no mesmo working tree antes do commit técnico.
* As alterações de 4D e 4E serão consolidadas em um único commit porque foram construídas sequencialmente nos mesmos arquivos antes de qualquer commit intermediário.
* Nenhum reset, checkout, stash ou reconstrução artificial do estado intermediário foi realizado.
* Nenhum código funcional foi alterado na Fase 4F; somente este arquivo de acompanhamento foi atualizado.
* `npx tsc --noEmit` foi validado com 0 erros no estado conjunto de 4D + 4E.
* A única falha global de `git diff --check` permanece constituída pelos 82 trailing whitespaces preexistentes nos dois arquivos consolidados, que continuam fora do escopo e fora do commit.
* A estrutura atual de `features/feed/`, os tipos compartilhados, os três consumidores do style, os quatro endpoints e as responsabilidades mantidas em FeedNoticias foram revisados no código real.
* Nenhuma outra extração interna do Feed está atualmente justificada.
* No checkpoint da Fase 4F, a Fase 4 permaneceu EM ANDAMENTO e sua validação final continuou pendente até o fechamento formal separado.
* No checkpoint da Fase 4F, a Fase 5 — Profile permaneceu NÃO INICIADA.

Subetapa Fase 4G — fechamento formal da Fase 4 — Feed:
* A estrutura final de `mobile/src/features/feed/` foi revisada no código real sem alteração de arquivos funcionais.
* FeedNoticias permanece a Screen/orquestradora responsável por estado principal, carregamento, perfil/raio, localização, lista de ocorrências, busca filtrada, refresh, ECO/força, denúncia, drawers e navegação.
* FeedControls permanece responsável somente pela UI de busca, limpeza, seletor/carrossel de modos, PROXIMIDADE/ECO, filtros e contador.
* OccurrenceCard permanece responsável pela UI individual de autor, foto, status, urgência, descrição, distância, tempo, contadores, ECO visual/animação e callbacks de interação.
* FeedBannerCarousel permanece um componente independente do domínio Feed.
* feed.types.ts contém somente os tipos compartilhados reais OcorrenciaFeed, FiltroFeed e ModoFeed, todos com reuso concreto.
* feed.utils.ts mantém normalizarTexto e ehUrgente por possuírem consumidores reais compartilhados.
* feed.styles.ts mantém uma única definição de feedButtonPressedStyle com `opacity: 0.78`, consumida por OccurrenceCard, FeedControls e FeedNoticias.
* O buttonPressed de RegistrarPet pertence ao domínio Pets, possui configuração diferente e não é consumidor do style do Feed.
* As operações GET `/auth/me`, GET `/ocorrencias/proximas`, POST `/ocorrencias/${occurrenceId}/forca` e POST `/ocorrencias/${denunciaOccurrenceId}/denuncias` permanecem em FeedNoticias, com endpoints, payloads e parâmetros preservados.
* ProfileQuickMenu e ProfileDetailScreen permanecem reservados à Fase 5 — Profile.
* OccurrenceDetailDrawer permanece reservado à Fase 7 — Occurrences.
* AppNavigationDrawer permanece reservado para avaliação definitiva na Fase 11 — Shared.
* services/api.ts permanece reservado à Fase 10 — Infrastructure; expo-location permanece sem reorganização nesta fase; theme/colors.ts permanece reservado à Fase 12.
* A decisão definitiva da Fase 4 é não criar atualmente FeedHeader, FeedEmptyState, FeedList, FeedFooter, FeedSearchBar, useFeed, useFeedSearch, useFeedFilters, useEco, feedService, occurrenceService, engagementService, reportService ou profileService.
* A análise do código real não demonstrou benefício arquitetural suficiente para novas extrações no escopo atual do Feed; carregarFeed permanece na Screen por coordenar múltiplas dependências, ECO/força poderá ser reavaliado na Fase 9 e denúncia poderá ser reavaliada em Reports quando aplicável.
* `npx tsc --noEmit` foi concluído com 0 erros como validação técnica final da Fase 4.
* `git diff --check` global retornou exit code 2 exclusivamente pelos 82 trailing whitespaces preexistentes nos dois arquivos consolidados, que permaneceram inalterados e fora do escopo.
* Os smoke tests das Fases 4A, 4B, 4C, 4D e 4E foram aprovados manualmente pelo usuário; o smoke 4E constituiu a validação funcional abrangente final da organização interna do Feed.
* Nenhum RF novo, backend, dependência, contrato de API ou comportamento foi alterado durante o fechamento formal.

Checklist final da Fase 4:
[X] FeedBannerCarousel reside em features/feed/components/
[X] FeedNoticias reside em features/feed/screens/
[X] OccurrenceCard extraído com fronteira real
[X] FeedControls extraído com fronteira visual real
[X] tipos compartilhados possuem reuso concreto
[X] utils compartilhados possuem reuso concreto
[X] feedButtonPressedStyle possui definição única
[X] FeedNoticias permanece Screen/orquestradora
[X] quatro operações HTTP preservadas
[X] payloads e parâmetros preservados
[X] localização preservada
[X] busca preservada
[X] filtros preservados
[X] PROXIMIDADE/ECO preservados
[X] ECO/força preservado
[X] denúncia preservada
[X] refresh preservado
[X] navegação preservada
[X] rota Feed preservada
[X] asset ECO preservado
[X] animação ECO preservada
[X] FeedBannerCarousel preservado
[X] componentes de outros domínios não movidos
[X] backend não alterado
[X] dependências não alteradas
[X] nenhum RF novo implementado
[X] nenhuma abstração sem uso real criada
[X] nenhuma pasta vazia criada
[X] nenhum .gitkeep criado
[X] TypeScript com 0 erros
[X] smoke tests 4A, 4B, 4C, 4D e 4E aprovados
[X] organização interna do Feed concluída
[X] validação final da Fase 4 concluída
[X] Fase 4 concluída

======================================================================
FASE 5 — PROFILE
================

Status:
CONCLUÍDA

Subetapas:
[X] ProfileQuickMenu migrado
[X] ProfileDetailScreen avaliado/migrado
[X] organização interna de Profile avaliada
[X] validação final da Fase 5

Subetapa Fase 5A — ProfileQuickMenu:
* Movido `mobile/src/components/ProfileQuickMenu.tsx` para `mobile/src/features/profile/components/ProfileQuickMenu.tsx`.
* `mobile/src/features/profile/components/` foi criado somente ao receber código real.
* Nome do arquivo e export default `ProfileQuickMenu` preservados.
* Contrato de props preservado integralmente, incluindo nomes, tipos e opcionalidade.
* JSX, styles, textos, ícones, accessibility, dimensões, Platform, sombras e comportamento visual preservados.
* Logout, Alert de confirmação, textos e ações Cancelar/Sair preservados.
* Callbacks `onClose`, `onOpenProfile` e `onOpenNotifications` e seus handlers preservados.
* Import relativo de theme ajustado de `../theme/colors` para `../../../theme/colors`, continuando a resolver para o mesmo módulo real `mobile/src/theme/colors.ts`.
* Import relativo de useAuthStore ajustado de `../store/useAuthStore` para `../../../store/useAuthStore`, continuando a resolver para o mesmo módulo real `mobile/src/store/useAuthStore.ts`.
* FeedNoticias teve somente o import de ProfileQuickMenu ajustado de `../../../components/ProfileQuickMenu` para `../../profile/components/ProfileQuickMenu`.
* MapScreen teve somente o import de ProfileQuickMenu ajustado de `../components/ProfileQuickMenu` para `../features/profile/components/ProfileQuickMenu`.
* ProfileDetailScreen não foi movido ou alterado.
* RegistrarPet não foi movido ou alterado.
* MapScreen não foi movido; somente seu import de ProfileQuickMenu foi ajustado.
* Nenhuma abstração adicional de Profile foi criada.
* Comparação normalizada do componente antes/depois, desconsiderando somente os imports relativos de theme e useAuthStore, confirmou conteúdo idêntico.
* `npx tsc --noEmit` concluído com 0 erros.
* Nenhum endpoint, contrato de API, backend, dependência, comportamento visual ou RF novo foi alterado.

Smoke test Fase 5A:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 5B — ProfileDetailScreen:
* Movido `mobile/src/screens/ProfileDetailScreen.tsx` para `mobile/src/features/profile/components/ProfileDetailScreen.tsx`.
* Nome do arquivo e export default `ProfileDetailScreen` preservados; nenhuma renomeação para Drawer foi realizada.
* `ProfileUpdateResult` permaneceu exportado pelo mesmo módulo do componente, com contrato integralmente preservado.
* `ProfileDetailProps` permaneceu interno ao componente, com tipos, opcionalidade e callbacks integralmente preservados.
* JSX, comportamento, Modal/Drawer, animações, tabs, perfil, edição, foto, raio, Pets, ocorrências, exclusão de conta, logout, estados, Alerts, validações, accessibility e styles preservados.
* Operações GET `/auth/me`, GET `/ocorrencias/minhas`, PUT `/auth/me`, POST `/auth/me/foto` e DELETE `/auth/me` preservadas sem alteração de endpoints, payloads, headers, FormData ou tratamento de erro.
* Import relativo de theme ajustado de `../theme/colors` para `../../../theme/colors`, continuando a resolver para `mobile/src/theme/colors.ts`.
* Import relativo de api ajustado de `../services/api` para `../../../services/api`, continuando a resolver para `mobile/src/services/api.ts`.
* Import relativo de useAuthStore ajustado de `../store/useAuthStore` para `../../../store/useAuthStore`, continuando a resolver para `mobile/src/store/useAuthStore.ts`.
* Import relativo de RegistrarPet ajustado de `./RegistrarPet` para `../../../screens/RegistrarPet`, continuando a resolver para `mobile/src/screens/RegistrarPet.tsx`.
* A dependência temporária de Profile com RegistrarPet no caminho legado foi preservada e permanece reservada para a Fase 6 — Pets.
* FeedNoticias teve somente o import de ProfileDetailScreen e `ProfileUpdateResult` ajustado.
* MapScreen teve somente o import de ProfileDetailScreen e `ProfileUpdateResult` ajustado; MapScreen não foi movido.
* App.tsx teve somente o import de ProfileDetailScreen ajustado; suas pendências preexistentes não foram limpas.
* ProfileQuickMenu permaneceu intacto no domínio Profile após aprovação manual da Fase 5A.
* RegistrarPet permaneceu em `mobile/src/screens/`, sem movimentação ou alteração.
* axios e expo-image-picker permaneceram importados e utilizados diretamente pelo componente sem reorganização.
* Nenhum service, hook, types ou abstração adicional de Profile foi criado.
* Comparação normalizada do componente antes/depois, desconsiderando somente os quatro imports relativos obrigatórios, confirmou 0 diferenças de conteúdo.
* `npx tsc --noEmit` concluído com 0 erros.

Smoke test Fase 5B:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 5C — profileService:
* Criado `mobile/src/features/profile/services/profileService.ts` com código real para as quatro operações HTTP próprias de Profile/conta.
* Centralizados GET `/auth/me`, PUT `/auth/me`, POST `/auth/me/foto` e DELETE `/auth/me` sem alteração de endpoints, payloads, headers ou respostas.
* GET `/ocorrencias/minhas` permaneceu diretamente em ProfileDetailScreen por pertencer ao domínio Occurrences e será reavaliado somente na Fase 7.
* Criado `mobile/src/features/profile/types/profile.types.ts` devido ao reuso concreto dos contratos entre Screen, service, Feed e Map.
* `ProfileUpdateResult`, `UserProfile` e `ProfileUpdatePayload` foram movidos literalmente para profile.types.ts e passaram a possuir definição única.
* `ProfileUpdateResult` permanece consumido por ProfileDetailScreen, FeedNoticias e MapScreen.
* `UserProfile` e `ProfileUpdatePayload` permanecem consumidos por ProfileDetailScreen e profileService.
* A preparação de `ProfileUpdatePayload`, incluindo trim, validações, Math.round e distinção entre PESSOA_FISICA e ONG, permaneceu na Screen.
* ImagePicker, permissões, seleção, edição, qualidade, asset, filename, MIME type e FormData permaneceram na Screen.
* O profileService recebe o FormData pronto e preserva `Content-Type: multipart/form-data`.
* O payload de exclusão `{ senha: deletePassword }` continua sendo construído na Screen e entregue pronto ao service.
* axios, Alerts e todo o tratamento de erros permaneceram na Screen; o service não captura ou converte erros.
* Estados, loading, refreshing, callbacks, tabs, Pets, Occurrences, JSX, styles e accessibility foram preservados.
* RegistrarPet e ProfileQuickMenu permaneceram intactos.
* Avatar, TabBar, InfoRow, PetSwitch e OcorrenciaCard permaneceram internos ao ProfileDetailScreen.
* FeedNoticias e MapScreen tiveram somente o import de tipo `ProfileUpdateResult` direcionado para profile.types.ts.
* App.tsx não foi alterado.
* Nenhum hook, componente visual, service de Occurrences/Pets/mídia ou abstração adicional foi criado.
* `npx tsc --noEmit` concluído com 0 erros.

Smoke test Fase 5C:
APROVADO MANUALMENTE PELO USUÁRIO.

O smoke test da Fase 5C constitui a validação funcional final da organização interna de Profile. Os smoke tests 5A, 5B e 5C foram aprovados manualmente pelo usuário; nenhum novo smoke foi solicitado no fechamento documental.

Subetapa Fase 5D — avaliação final e fechamento formal da Fase 5:
* A estrutura final de `mobile/src/features/profile/` foi revisada no código real e contém somente `components/ProfileDetailScreen.tsx`, `components/ProfileQuickMenu.tsx`, `services/profileService.ts` e `types/profile.types.ts`.
* Os diretórios components, services e types contêm código real; nenhuma pasta vazia, arquivo `.gitkeep` ou `index.ts` artificial existe no domínio Profile.
* Avatar possui uma instanciação e permanece componente interno de ProfileDetailScreen.
* TabBar possui uma instanciação e permanece componente interno de ProfileDetailScreen.
* InfoRow possui nove instanciações, todas dentro de ProfileDetailScreen; já constitui a abstração interna que elimina a repetição relevante.
* PetSwitch possui uma instanciação e permanece componente interno de ProfileDetailScreen.
* OcorrenciaCard possui uma instanciação e permanece componente interno de ProfileDetailScreen.
* Decisão arquitetural: não extrair atualmente ProfileAvatar.tsx, ProfileTabBar.tsx, ProfileInfoRow.tsx, ProfilePetSwitch.tsx ou ProfileOccurrenceCard.tsx, nem equivalentes com outros nomes.
* Todos esses componentes pertencem exclusivamente à composição interna de ProfileDetailScreen, não possuem consumidores externos e compartilham constantes e/ou StyleSheet da própria composição. A extração criaria arquivos, imports e possíveis tipos/styles compartilhados sem ganho arquitetural real.
* A permanência desses componentes internos é decisão arquitetural definitiva da Fase 5 e não constitui pendência.
* O nome `ProfileDetailScreen` foi preservado para reduzir risco. Embora o componente se comporte visualmente como Modal/Drawer, eventual renomeação será reavaliada apenas em Cleanup se houver benefício real; a ausência da renomeação não impede o fechamento da Fase 5.
* `mobile/src/features/profile/services/profileService.ts` permanece a abstração justificada da Fase 5 e centraliza somente GET `/auth/me`, PUT `/auth/me`, POST `/auth/me/foto` e DELETE `/auth/me`.
* As quatro operações próprias de Profile não permanecem diretamente em ProfileDetailScreen.
* profileService usa o cliente HTTP existente, não conhece UI ou Zustand, não realiza Alerts, não captura ou converte erros, não prepara payload de formulário e não utiliza ImagePicker.
* GET `/ocorrencias/minhas` permanece diretamente em ProfileDetailScreen. A operação pertence conceitualmente a Occurrences e será reavaliada na Fase 7; essa dependência temporária não impede o fechamento de Profile.
* `profile.types.ts` contém somente ProfileUpdateResult, UserProfile e ProfileUpdatePayload, todos justificados por reuso concreto e com definição única.
* ProfileUpdateResult é consumido por ProfileDetailScreen, FeedNoticias e MapScreen.
* UserProfile é consumido por ProfileDetailScreen e profileService.
* ProfileUpdatePayload é consumido por ProfileDetailScreen e profileService.
* RegistrarPet continua renderizado por ProfileDetailScreen e permanece inalterado em `mobile/src/screens/RegistrarPet.tsx`; a dependência temporária Profile -> RegistrarPet será resolvida na Fase 6 — Pets.
* ProfileQuickMenu permanece autocontido, consumido por Feed e Map, com logout e contratos preservados; nenhuma organização adicional foi justificada.
* expo-image-picker permanece diretamente em ProfileDetailScreen e sua organização tecnológica continua reservada para a Fase 10 — Infrastructure.
* useAuthStore permanece em `mobile/src/store/useAuthStore.ts` como dependência transversal preservada.
* As pendências preexistentes AppStack, createNativeStackNavigator, ActivityIndicator e import de ProfileDetailScreen em App.tsx foram preservadas para fases posteriores, especialmente Cleanup/App.
* `npx tsc --noEmit` foi concluído com 0 erros como validação técnica final da Fase 5.
* `git diff --check` global retornou exit code 2 exclusivamente por 98 ocorrências atuais de trailing whitespace nos consolidados fora do escopo: 49 em codigo_frontend_consolidado.txt e 49 em codigo_projeto_consolidado.txt.
* Os registros históricos da Fase 4 que mencionam 82 ocorrências não foram reescritos.
* Nenhum arquivo funcional, backend, dependência, contrato de API, configuração funcional, comportamento ou RF foi alterado durante a Fase 5D.

Checklist final da Fase 5:
[X] ProfileQuickMenu reside em features/profile/components/
[X] ProfileDetailScreen reside em features/profile/components/
[X] nomes preservados durante movimentações
[X] ProfileUpdateResult preservado
[X] ProfileDetailProps preservado
[X] profileService criado com fronteira real
[X] quatro operações próprias de Profile centralizadas
[X] /ocorrencias/minhas mantido fora do profileService
[X] profile.types.ts criado somente por reuso concreto
[X] UserProfile possui definição única
[X] ProfileUpdatePayload possui definição única
[X] ProfileUpdateResult possui definição única
[X] preparação dos payloads permanece na UI
[X] ImagePicker permanece na UI
[X] FormData permanece na UI
[X] axios/tratamento de erro permanece na UI
[X] ProfileQuickMenu preservado
[X] componentes internos avaliados
[X] nenhuma extração visual adicional justificada
[X] dependência de RegistrarPet registrada para Fase 6
[X] dependência de /ocorrencias/minhas registrada para Fase 7
[X] App.tsx não limpo prematuramente
[X] services/api.ts preservado
[X] useAuthStore preservado
[X] theme preservado
[X] backend não alterado
[X] dependências não alteradas
[X] nenhum RF novo implementado
[X] nenhuma abstração sem uso real criada
[X] nenhuma pasta vazia criada
[X] nenhum .gitkeep criado
[X] TypeScript com 0 erros
[X] smoke tests 5A, 5B e 5C aprovados
[X] organização interna de Profile concluída
[X] validação final da Fase 5 concluída
[X] Fase 5 concluída

======================================================================
FASE 6 — PETS
=============

Status:
CONCLUÍDA

Subetapas:
[X] RegistrarPet movido para o domínio Pets
[X] smoke test manual da Fase 6A
[X] petService e pet.types extraídos
[X] smoke test manual da Fase 6B
[X] PetCard extraído
[X] smoke test manual da Fase 6C
[X] PetDetailModal extraído
[X] smoke test manual da Fase 6D
[X] PetFormModal extraído
[X] smoke test manual da Fase 6E
[X] RegistrarPet renomeado semanticamente para PetsTab
[X] smoke test manual da Fase 6F
[X] organização interna de Pets avaliada
[X] validação final da Fase 6
[X] fechamento formal da Fase 6G

Subetapa Fase 6A — movimentação estrutural de RegistrarPet:
* Movido `mobile/src/screens/RegistrarPet.tsx` para `mobile/src/features/pets/components/RegistrarPet.tsx`.
* `mobile/src/features/pets/components/` foi criado somente ao receber código real.
* Nome do arquivo RegistrarPet.tsx preservado.
* Export `export default function RegistrarPet()` preservado.
* ProfileDetailScreen foi o único consumidor executável identificado e teve somente o caminho do import atualizado de `../../../screens/RegistrarPet` para `../../pets/components/RegistrarPet`.
* O JSX `<RegistrarPet />` permaneceu funcionalmente idêntico.
* Os imports relativos de api, theme e navigation.types foram ajustados de `../services/api`, `../theme/colors` e `../navigation/navigation.types` para `../../../services/api`, `../../../theme/colors` e `../../../navigation/navigation.types`, continuando a resolver exatamente para os mesmos módulos reais.
* Imports de React, React Native, React Navigation, ícones e expo-image-picker foram preservados.
* Comparação normalizada do conteúdo antes/depois, desconsiderando somente os três imports relativos obrigatórios, confirmou 1.509 linhas e 0 diferenças.
* Listagem, cadastro, formulário, modal de cadastro, lista/cards, modal de detalhes, seleção de foto, FormData, textos, Alerts, console.error, acessibilidade, estilos, cores, tamanhos, estados, validações, payload, headers, FlatList e comportamento foram preservados.
* GET `/pets/meus` e POST `/pets/` foram preservados sem alteração de endpoint, payload, FormData multipart ou headers.
* A integração “INFORMAR COMO PERDIDO” e a navegação para CadastroOcorrencia com os dados preexistentes do pet foram preservadas.
* O arquivo antigo não permanece duplicado; `mobile/src/features/pets/components/RegistrarPet.tsx` é a única implementação executável.
* A ausência de edição e exclusão de pet foi confirmada como comportamento atual preexistente. O frontend e o router atual de Pets possuem somente POST `/pets/` e GET `/pets/meus`; nenhum RF, endpoint ou abstração de edição/exclusão foi criado.
* Backend, services/api.ts, MapScreen, FeedNoticias, CadastroOcorrenciaScreen, navigation.types.ts, theme/colors.ts e App.tsx não foram alterados.
* expo-image-picker e FormData permanecem diretamente em RegistrarPet; sua reorganização não pertence à Fase 6A.
* Pet, TipoEspecie, FotoSelecionada, DetailRow, carregarPets, cadastrarPet, selecionarFoto, informarComoPerdido, JSX e StyleSheet permanecem no mesmo arquivo.
* Nenhum PetsTab, PetCard, PetList, PetFormModal, PetPhotoPicker, usePets, petService, pet.types, styles, constants, utils, index.ts ou `.gitkeep` foi criado.
* `npx tsc --noEmit` concluído com 0 erros.
* `git diff --check` global retornou exit code 2 exclusivamente pelas 98 ocorrências preexistentes de trailing whitespace nos consolidados fora do escopo: 49 em codigo_frontend_consolidado.txt e 49 em codigo_projeto_consolidado.txt.

Smoke test Fase 6A:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 6B — petService e pet.types:
* Criado `mobile/src/features/pets/types/pet.types.ts` somente com o contrato compartilhado real Pet.
* Pet foi movido literalmente de RegistrarPet para pet.types.ts, preservando todos os campos, tipos, nulabilidade e comentários preexistentes.
* Pet passou a possuir definição única e dois consumidores reais: RegistrarPet e petService.
* TipoEspecie, FotoSelecionada e DetailRowProps permanecem locais em RegistrarPet porque continuam possuindo somente consumidor interno.
* Nenhum PetCreatePayload, PetUpdatePayload, PetFormData, PetResponse, PetLostPayload, PetDetail ou outro contrato antecipado foi criado.
* Criado `mobile/src/features/pets/services/petService.ts` usando o cliente HTTP existente de `mobile/src/services/api.ts`.
* petService expõe somente os métodos getMyPets e createPet.
* GET `/pets/meus` e POST `/pets/` foram centralizados em petService sem alteração de endpoint, método HTTP, tipo de resposta ou configuração multipart.
* RegistrarPet deixou de importar diretamente api.ts e não realiza mais chamadas diretas api.get ou api.post relativas a Pets.
* petService é o único ponto do frontend executável responsável por GET `/pets/meus` e POST `/pets/`.
* A montagem integral do FormData permanece em RegistrarPet, incluindo normalização de nome, resolução de espécie, campos opcionais, porte, idade, foto e o `as any` preexistente do objeto React Native.
* expo-image-picker, permissões, seleção da foto, edição, qualidade, filename e MIME type permanecem diretamente em RegistrarPet.
* informarComoPerdido, CadastroOcorrencia e todos os campos do parâmetro pet permanecem diretamente em RegistrarPet e foram preservados.
* Estados, loading, Alerts, logs, validações, atualização local da lista, modais, JSX, StyleSheet, textos, ícones, accessibility, cores, sombras e comportamento não foram alterados.
* Backend, Profile, Feed, Map, Occurrences, Auth, navigation.types.ts, services/api.ts, theme/colors.ts, useAuthStore, App.tsx, dependências e package.json não foram alterados.
* Nenhuma edição ou exclusão de pet foi implementada; nenhum endpoint adicional foi criado.
* Nenhum componente visual, hook, styles separado, utils, constants, index.ts ou `.gitkeep` foi criado.
* `npx tsc --noEmit` concluído com 0 erros.
* `git diff --check` global retornou exit code 2 exclusivamente pelas 98 ocorrências preexistentes de trailing whitespace nos consolidados fora do escopo: 49 em codigo_frontend_consolidado.txt e 49 em codigo_projeto_consolidado.txt.

Smoke test Fase 6B:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 6C — PetCard:
* A extração foi justificada por uma fronteira visual real: o renderItem representava exclusivamente um Pet e apenas abria os detalhes do item selecionado.
* Criado `mobile/src/features/pets/components/PetCard.tsx` como componente visual stateless.
* PetCard recebe somente as props `pet: Pet` e `onPress: () => void`.
* O contrato Pet existente em `features/pets/types/pet.types.ts` foi reutilizado sem alteração.
* O JSX do Pressable do card foi extraído de RegistrarPet, preservando foto, placeholder, escolha de ícone, nome, espécie, raça, porte, chevron, accessibility e ação de abertura dos detalhes.
* Comparação normalizada do JSX confirmou 47 linhas antes e depois e 0 diferenças após somente `item` -> `pet` e substituição do callback do pai pela prop onPress.
* Os styles exclusivos petCard, petImageWrapper, petImage, petPlaceholder, petInfo, petName, petMeta e petSize foram movidos literalmente para o StyleSheet local de PetCard.
* O efeito buttonPressed foi definido localmente em PetCard com `opacity: 0.85` e `scale: 0.99`; a definição original permanece em RegistrarPet por ainda possuir dois consumidores reais: Adicionar pet e INFORMAR COMO PERDIDO.
* RegistrarPet passou a renderizar `<PetCard pet={item} onPress={() => setPetSelecionado(item)} />`; petSelecionado e setPetSelecionado permanecem no componente pai.
* PetCard não possui estado, hooks, chamadas HTTP ou imports de api, petService, navigation ou expo-image-picker.
* O formulário, o modal de cadastro, FormData, expo-image-picker, seleção de foto, validações e cadastrarPet permaneceram integralmente em RegistrarPet.
* O modal de detalhes, DetailRow, DetailRowProps, informarComoPerdido e a navegação para CadastroOcorrencia permaneceram integralmente em RegistrarPet.
* petService.ts e pet.types.ts não foram alterados.
* GET `/pets/meus`, POST `/pets/`, multipart e contratos HTTP permaneceram inalterados.
* Backend, Profile, Feed, Map, Occurrences, Auth, navigation.types.ts, services/api.ts, theme/colors.ts, App.tsx, dependências e package.json não foram alterados.
* Nenhum RF novo, componente visual adicional, hook, style compartilhado, utils, constants, index.ts ou `.gitkeep` foi criado.
* `npx tsc --noEmit` concluído com 0 erros.
* `git diff --check` global retornou exit code 2 exclusivamente pelas 98 ocorrências preexistentes de trailing whitespace nos consolidados fora do escopo: 49 em codigo_frontend_consolidado.txt e 49 em codigo_projeto_consolidado.txt.

Smoke test Fase 6C:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 6D — PetDetailModal:
* A extração foi justificada por uma fronteira visual autocontida dependente somente do Pet selecionado, da ação de fechar e da ação de informar aquele Pet como perdido.
* Criado `mobile/src/features/pets/components/PetDetailModal.tsx` como componente apresentacional stateless.
* PetDetailModal recebe somente as props `pet: Pet | null`, `onClose: () => void` e `onReportLost: (pet: Pet) => void`.
* O contrato Pet existente em `features/pets/types/pet.types.ts` foi reutilizado sem alteração.
* O JSX completo do Modal de detalhes foi removido de RegistrarPet e movido para PetDetailModal, preservando visible, transparência, animação, onRequestClose, botão de fechar, foto, placeholder, nome, linhas, fallbacks, botão de pet perdido, textos e accessibility.
* Comparação normalizada do modal confirmou 130 linhas antes e depois e 0 diferenças após somente petSelecionado -> pet, callbacks de fechamento -> onClose e informarComoPerdido -> onReportLost.
* DetailRow e DetailRowProps foram movidos para PetDetailModal como implementação interna não exportada.
* Os styles exclusivos detailContainer, detailHeader, detailTitle, detailPhotoWrapper, detailPhoto, detailPlaceholder, detailPetName, detailCard, detailRow, detailLabel, detailValue, lostPetButton, lostPetButtonContent, lostPetButtonTitle e lostPetButtonSubtitle foram movidos literalmente.
* A comparação individual dos 15 styles exclusivos e dos três styles compartilhados confirmou 0 diferenças.
* modalBackdrop, closeButton e buttonPressed receberam definições locais equivalentes em PetDetailModal; suas definições originais permanecem em RegistrarPet porque o modal de formulário e o botão Adicionar pet continuam sendo consumidores reais.
* RegistrarPet passou a renderizar `<PetDetailModal pet={petSelecionado} onClose={() => setPetSelecionado(null)} onReportLost={informarComoPerdido} />`.
* petSelecionado e setPetSelecionado permanecem em RegistrarPet.
* informarComoPerdido e navigation.navigate para CadastroOcorrencia permanecem integralmente em RegistrarPet, com id_pet, nome, especie, raca, sexo, cor, porte, idade e foto preservados.
* PetDetailModal não possui estado de domínio, hooks, chamadas HTTP ou imports de navigation, CadastroOcorrencia, petService, api ou expo-image-picker.
* O formulário, modal de cadastro, FormData, expo-image-picker, validações, setters e cadastrarPet permaneceram integralmente em RegistrarPet.
* PetCard.tsx, petService.ts e pet.types.ts não foram alterados.
* GET `/pets/meus`, POST `/pets/`, multipart e contratos HTTP permaneceram inalterados.
* Backend, Profile, Feed, Map, Occurrences, Auth, navigation.types.ts, services/api.ts, theme/colors.ts, App.tsx, dependências e package.json não foram alterados.
* Nenhum RF novo, componente adicional, hook, style compartilhado, utils, constants, index.ts ou `.gitkeep` foi criado.
* `npx tsc --noEmit` concluído com 0 erros.
* `git diff --check` global retornou exit code 2 exclusivamente pelas 98 ocorrências preexistentes de trailing whitespace nos consolidados fora do escopo: 49 em codigo_frontend_consolidado.txt e 49 em codigo_projeto_consolidado.txt.

Smoke test Fase 6D:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 6E — PetFormModal:
* A extração foi justificada pela coesão do formulário: campos, foto, validação, reset, submissão e estado de saving pertencem ao mesmo fluxo e sua separação puramente visual exigiria props e setters excessivos.
* Criado `mobile/src/features/pets/components/PetFormModal.tsx` com as props `visible: boolean`, `onClose: () => void` e `onPetCreated: (pet: Pet) => void`.
* RegistrarPet permanece proprietário de `formVisible` e decide a abertura e o fechamento da composição; PetFormModal não internaliza a visibilidade.
* RegistrarPet permanece proprietário de `pets` e `setPets`, do carregamento inicial e da atualização da lista após a criação.
* Os estados saving, nome, especie, especieOutro, raca, sexo, cor, porte, idade e foto foram movidos para PetFormModal com os mesmos tipos e valores iniciais.
* TipoEspecie e FotoSelecionada foram confirmados como exclusivos do formulário e movidos para PetFormModal como tipos internos não exportados; `pet.types.ts` permaneceu inalterado.
* limparFormulario foi movido literalmente, preservando todos os valores de reset.
* fecharFormulario permanece interno ao formulário e preserva a ordem reset -> fechamento, delegando somente o fechamento ao callback `onClose`.
* selecionarFoto e o import direto de expo-image-picker foram movidos para PetFormModal, preservando permissão, mensagem, opções do seletor, cancelamento, filename, MIME type, logs e Alert de erro.
* cadastrarPet foi movido para PetFormModal, preservando normalização, validações, trim, campos opcionais, FormData, nomes dos campos, foto, tratamento de erro, logs, Alerts e saving.
* `petService.createPet(formData)` passou a ser consumido por PetFormModal; `petService.getMyPets()` permanece consumido por RegistrarPet.
* O FormData, o objeto React Native da foto e o `as any` preexistente foram preservados integralmente.
* Após sucesso, PetFormModal executa `onPetCreated(response.data)`, fecha/reseta o formulário e então mostra o mesmo Alert, preservando a ordem observável preexistente.
* O callback síncrono do pai acrescenta o Pet criado no início da lista por meio de `setPets((petsAtuais) => [pet, ...petsAtuais])`.
* O JSX completo do modal de formulário foi movido para PetFormModal, sem alteração de textos, campos, opções, condicionais, loading, ícones ou accessibility.
* Os styles exclusivos modalBackdrop, modalContainer, modalHeader, modalTitle, modalSubtitle, closeButton, photoSelector, selectedPhoto, photoTitle, photoHint, label, inputContainer, input, optionContainer, optionButton, sizeButton, optionButtonActive, optionText, optionTextActive, saveButton, saveButtonDisabled e saveButtonText foram movidos literalmente.
* buttonPressed permaneceu em RegistrarPet para o botão Adicionar pet; nenhum style compartilhado foi criado.
* RegistrarPet deixou de importar Image, Modal, ScrollView, TextInput e expo-image-picker porque esses símbolos não possuem mais consumidor no pai; ActivityIndicator e Alert permanecem devido ao carregamento inicial.
* PetCard.tsx e PetDetailModal.tsx permaneceram intactos.
* informarComoPerdido e a navegação para CadastroOcorrencia permaneceram em RegistrarPet com id_pet, nome, especie, raca, sexo, cor, porte, idade e foto preservados.
* GET `/pets/meus` e POST `/pets/`, métodos, multipart, payload e contratos permaneceram inalterados; componentes de Pets não importam api diretamente.
* Backend, dependências, theme, navigation.types.ts e contratos de API permaneceram intactos.
* Nenhum RF novo, edição, exclusão, endpoint, hook, lista adicional, PhotoPicker, style compartilhado, pasta vazia, index.ts ou `.gitkeep` foi criado.
* Comparações normalizadas confirmaram equivalência de limparFormulario, fecharFormulario, selecionarFoto, cadastrarPet, JSX do formulário e todos os styles movidos, considerando somente as delegações previstas por onClose e onPetCreated.
* `npx tsc --noEmit` concluído com 0 erros.
* `git diff --check` global retornou exit code 2 exclusivamente pelas 98 ocorrências preexistentes de trailing whitespace nos consolidados fora do escopo: 49 em codigo_frontend_consolidado.txt e 49 em codigo_projeto_consolidado.txt; a verificação restrita aos arquivos rastreados da Fase 6E concluiu sem erros.
* `git diff --cached --check` concluído sem erros para os três arquivos do checkpoint técnico da Fase 6E; os consolidados permaneceram fora do staging.

Smoke test Fase 6E:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 6F — renomeação semântica PetsTab:
* A renomeação foi justificada porque o componente deixou de representar somente o cadastro e atualmente coordena a aba de Pets dentro de Profile.
* RegistrarPet não é uma Screen nem uma rota; o código real confirma que ele é renderizado exclusivamente como conteúdo da aba Pets de ProfileDetailScreen.
* A pesquisa em todo o código executável encontrou um único consumidor real: `mobile/src/features/profile/components/ProfileDetailScreen.tsx`.
* `mobile/src/features/pets/components/RegistrarPet.tsx` foi renomeado para `mobile/src/features/pets/components/PetsTab.tsx`, sem deixar arquivo antigo ou implementação duplicada.
* O componente `export default function RegistrarPet()` foi renomeado para `export default function PetsTab()`.
* ProfileDetailScreen passou a importar PetsTab de `../../pets/components/PetsTab` e a renderizar `<PetsTab />`.
* A condição `activeTab === "pets" && mostrarAbaPets`, o KeyboardAvoidingView, perfilContent, minhasOcorrencias e todas as demais abas permaneceram intactos.
* Comparação normalizada entre o RegistrarPet anterior e PetsTab confirmou conteúdo integralmente idêntico após desconsiderar somente o caminho do arquivo e `RegistrarPet` -> `PetsTab` no export.
* Os prefixos históricos de log `[RegistrarPet]` foram preservados literalmente para manter o menor diff funcional possível.
* Carregamento, estados, FlatList, cabeçalho, PetCard, PetFormModal, PetDetailModal, integração de pet perdido, navegação, nove campos de prefill, loading, Alerts, textos, accessibility, styles e COLORS permaneceram inalterados.
* PetCard.tsx, PetDetailModal.tsx, PetFormModal.tsx, petService.ts e pet.types.ts permaneceram intactos.
* GET `/pets/meus` e POST `/pets/`, métodos, multipart, payload e contratos permaneceram inalterados.
* Backend, dependências, theme, navigation.types.ts e contratos de API permaneceram intactos.
* Nenhum RF novo, edição, exclusão, update, endpoint ou migration foi criado.
* PetList.tsx: NÃO CRIADO — RegistrarPet/PetsTab já representa a própria aba/lista e um wrapper do FlatList não apresentaria benefício arquitetural comprovado.
* PetPhotoPicker.tsx: NÃO CRIADO — a seleção de foto possui somente PetFormModal como consumidor; sua abstração tecnológica permanece reservada para a Fase 10 — Infrastructure / media.
* usePets.ts: NÃO CRIADO — a lógica restante do coordenador é pequena, específica da aba e sem reuso concreto.
* Nenhum diretório hooks, styles, utils ou constants, nenhum index.ts, `.gitkeep`, wrapper ou barrel export foi criado.
* `npx tsc --noEmit` concluído com 0 erros.
* `git diff --check` global retornou exit code 2 exclusivamente pelas 98 ocorrências preexistentes de trailing whitespace nos consolidados fora do escopo: 49 em codigo_frontend_consolidado.txt e 49 em codigo_projeto_consolidado.txt; a verificação restrita aos arquivos rastreados da Fase 6F concluiu sem erros.
* `git diff --cached --check` concluído sem erros para o rename e os arquivos documental/Profile da Fase 6F; os consolidados permaneceram fora do staging.

Smoke test Fase 6F:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 6G — fechamento formal da Fase 6:
* A Fase 6 foi encerrada formalmente após a aprovação manual dos smoke tests 6A, 6B, 6C, 6D, 6E e 6F.
* A estrutura final validada do domínio Pets é:
  mobile/src/features/pets/
  ├── components/
  │   ├── PetCard.tsx
  │   ├── PetDetailModal.tsx
  │   ├── PetFormModal.tsx
  │   └── PetsTab.tsx
  ├── services/
  │   └── petService.ts
  └── types/
      └── pet.types.ts
* PetsTab coordena o carregamento e a lista de pets, o loading, a abertura do formulário, o pet selecionado, a composição de PetCard, PetFormModal e PetDetailModal e a integração pet perdido → CadastroOcorrencia.
* PetCard permanece responsável exclusivamente pela apresentação do card.
* PetDetailModal permanece responsável pela apresentação dos detalhes, fechamento e callback de informar pet perdido.
* PetFormModal concentra estado, validações, foto, ImagePicker, FormData, criação, reset e saving do formulário.
* petService concentra somente GET `/pets/meus` e POST `/pets/`.
* pet.types contém o contrato compartilhado Pet.
* PetList.tsx NÃO FOI CRIADO: PetsTab já coordena a lista e o FlatList não possui complexidade ou reuso suficiente para justificar outro wrapper.
* PetPhotoPicker.tsx NÃO FOI CRIADO: a seleção de foto possui somente PetFormModal como consumidor; eventual abstração do expo-image-picker permanece reservada para a Fase 10 — Infrastructure.
* usePets.ts NÃO FOI CRIADO: a lógica restante de PetsTab é pequena, específica da aba e sem reuso concreto.
* PetDetailScreen.tsx NÃO FOI CRIADO: não existe rota independente de detalhes de Pet no comportamento atual.
* A organização interna de Pets foi avaliada e nenhuma nova extração está justificada atualmente.
* A edição de Pet não existe no comportamento/backend atual; sua ausência é preexistente, não constitui falha desta refatoração, não motivou RF novo e nenhum endpoint foi inventado.
* Critérios finais confirmados:
  [X] lista de pets preservada
  [X] cadastro preservado
  [X] fluxo de foto preservado
  [X] integração “pet perdido” → ocorrência preservada
  [X] GET `/pets/meus` preservado
  [X] POST `/pets/` preservado
  [X] multipart preservado
  [X] payloads preservados
  [X] CadastroOcorrencia prefill preservado
  [X] backend não alterado
  [X] Mapbox não alterado
  [X] dependências não alteradas
  [X] nenhum RF novo
  [X] nenhuma abstração sem uso real criada
  [X] nenhuma pasta vazia criada
  [X] nenhum `.gitkeep` criado
* Esta subetapa de fechamento alterou somente STATUS_REFATORACAO_PETRADAR.txt; nenhum arquivo de código-fonte foi alterado.
* `npx tsc --noEmit` foi executado uma vez no fechamento e concluído com 0 erros.

======================================================================
FASE 7 — OCCURRENCES
====================

Status:
EM ANDAMENTO

Subetapas:
[X] OccurrenceDetailDrawer movido para o domínio Occurrences
[X] validação técnica da Fase 7A
[X] smoke test manual da Fase 7A
[X] CadastroOcorrenciaScreen movida para o domínio Occurrences
[X] validação técnica da Fase 7B
[X] smoke test manual da Fase 7B
[X] OcorrenciaResumo extraído para occurrence.types
[X] validação técnica da Fase 7C
[X] smoke test manual da Fase 7C
[X] leituras de Occurrences centralizadas em occurrenceService
[X] validação técnica da Fase 7D
[X] smoke test manual da Fase 7D
[X] escritas nucleares de Occurrences centralizadas em occurrenceService
[X] validação técnica da Fase 7E
[X] smoke test manual da Fase 7E
[X] OccurrenceTypeSection extraído para components/form
[X] TipoOcorrencia compartilhado via occurrence.types
[X] validação técnica da Fase 7F
[X] smoke test manual da Fase 7F
[X] AnimalTypeSelector extraído para components/form
[X] TipoAnimal compartilhado via occurrence.types
[X] validação técnica da Fase 7G
[X] smoke test manual da Fase 7G
[X] ChoiceButton extraído para components/form
[X] validação técnica da Fase 7H
[X] smoke test manual da Fase 7H
[X] SelectionChip extraído para components/form
[X] validação técnica da Fase 7I
[X] smoke test manual da Fase 7I
[X] SelectionChipGroup extraído para components/form
[X] validação técnica da Fase 7J
[X] smoke test manual da Fase 7J
[X] estilos compartilhados do formulário centralizados
[X] validação técnica da Fase 7K
[X] smoke test manual da Fase 7K
[X] OccurrenceFormSection extraído para components/form
[X] validação técnica da Fase 7L
[X] smoke test manual da Fase 7L
[X] AnimalSection extraído para components/form
[X] validação técnica da Fase 7M
[X] smoke test manual da Fase 7M
[X] HealthSection extraído para components/form
[X] validação técnica da Fase 7N
[X] smoke test manual da Fase 7N
[X] DisabilitySection extraído para components/form
[X] validação técnica da Fase 7O
[X] smoke test manual da Fase 7O
[X] InitialCareSection extraído para components/form
[X] validação técnica da Fase 7P
[ ] smoke test manual da Fase 7P
[ ] validação final da Fase 7

Subetapa Fase 7A — movimentação estrutural de OccurrenceDetailDrawer:
* Movido `mobile/src/components/OccurrenceDetailDrawer.tsx` para `mobile/src/features/occurrences/components/OccurrenceDetailDrawer.tsx`.
* `mobile/src/features/occurrences/` e `mobile/src/features/occurrences/components/` foram criados somente para receber código real.
* Nome do arquivo, nome do componente, export default, props, tipos locais, hooks, estados, JSX, styles, textos, accessibility, Alerts, animações e comportamento foram preservados.
* A pesquisa no código executável confirmou exatamente dois consumidores reais: `mobile/src/features/feed/screens/FeedNoticias.tsx` e `mobile/src/screens/MapScreen.tsx`.
* FeedNoticias passou a importar o drawer de `../../occurrences/components/OccurrenceDetailDrawer`.
* MapScreen passou a importar o drawer de `../features/occurrences/components/OccurrenceDetailDrawer`.
* Os imports internos de api, useAuthStore e theme foram ajustados exclusivamente por necessidade física para `../../../services/api`, `../../../store/useAuthStore` e `../../../theme/colors`, continuando a resolver os mesmos módulos reais.
* Comparação normalizada confirmou 2.702 linhas e conteúdo integralmente idêntico após desconsiderar somente o caminho físico e os três imports relativos internos obrigatórios.
* Chamadas HTTP, URLs, métodos, payloads, headers, tratamento de resposta, ordem das chamadas, edição, exclusão, cuidados e histórico permaneceram inalterados.
* FeedNoticias teve somente o caminho do import alterado; Feed, filtros, ECO, denúncia, busca, cards, handlers, estados, endpoints e styles permaneceram intactos.
* MapScreen teve somente o caminho do import alterado; lógica, integração e comportamento do Mapbox permaneceram intactos.
* `mobile/src/screens/CadastroOcorrenciaScreen.tsx` permaneceu no local atual e não foi alterado.
* ProfileDetailScreen e o acesso GET `/ocorrencias/minhas` não foram alterados.
* Backend, dependências, contratos de API e funcionalidades existentes não foram alterados.
* Nenhum RF novo, service, type, hook, util, constant, pasta vazia, index.ts ou `.gitkeep` foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7A foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7A:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7B — movimentação estrutural de CadastroOcorrenciaScreen:
* Movido `mobile/src/screens/CadastroOcorrenciaScreen.tsx` para `mobile/src/features/occurrences/screens/CadastroOcorrenciaScreen.tsx`.
* `mobile/src/features/occurrences/screens/` foi criado somente para receber código real.
* Nome do arquivo, componente, export default, props, tipagem da rota, estados, hooks, callbacks, formulário, validações, JSX, styles, textos, Alerts, accessibility e comportamento foram preservados.
* A pesquisa no código executável confirmou um único consumidor real: `mobile/src/navigation/AppNavigator.tsx`.
* AppNavigator passou a importar a Screen de `../features/occurrences/screens/CadastroOcorrenciaScreen`.
* A rota `CadastroOcorrencia`, sua ordem, componente, AppTabParamList, screenOptions, tabBar e parâmetros permaneceram inalterados.
* Os imports internos de theme, useAuthStore, api e AppTabParamList foram ajustados exclusivamente por necessidade física para `../../../theme/colors`, `../../../store/useAuthStore`, `../../../services/api` e `../../../navigation/navigation.types`, continuando a resolver os mesmos módulos reais.
* Comparação normalizada confirmou 3.417 linhas e conteúdo integralmente idêntico após desconsiderar somente o caminho físico e os quatro imports relativos internos obrigatórios.
* Expo Location foi preservado integralmente, incluindo permissões, precisão, callbacks, fallbacks, localização atual e tratamento de erro.
* Expo ImagePicker foi preservado integralmente, incluindo permissões, launchImageLibraryAsync, edição, qualidade, seleção, cancelamento, preview e FormData da foto.
* Mapbox e geocoding foram preservados, incluindo `EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN`, endpoint, parâmetros e fetch atuais.
* Os modos de criação e edição, o FormData, multipart, payloads, validações e tratamento de erro permaneceram inalterados.
* O prefill Pet → INFORMAR COMO PERDIDO → CadastroOcorrencia, route.params.pet, PetOcorrenciaPrefill e todos os seus campos permaneceram inalterados.
* GET `/ocorrencias/{id}`, POST `/ocorrencias/` e PUT `/ocorrencias/{id}` foram preservados sem mudança de método, URL, payload, multipart ou ordem.
* OccurrenceDetailDrawer, FeedNoticias, MapScreen, ProfileDetailScreen e GET `/ocorrencias/minhas` permaneceram intactos.
* Backend, dependências, contratos de API e funcionalidades existentes não foram alterados.
* Nenhum RF novo, service, type, hook, formulário extraído, util, constant, style compartilhado, pasta vazia, index.ts ou `.gitkeep` foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7B foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7B:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7C — occurrence.types / tipo-base compartilhado:
* Criado `mobile/src/features/occurrences/types/occurrence.types.ts` somente para receber o contrato compartilhado real `OcorrenciaResumo`.
* OcorrenciaResumo contém exclusivamente os oito campos comuns confirmados nas três representações atuais: id_ocorrencia, tipo_ocorrencia, status_badge, tipo_animal, foto, nivel_urgencia, data_ocorrencia e endereco_localizacao.
* `endereco_localizacao?: string | null` preserva o formato mais completo já aceito pelo código executável, sem conversão, normalização ou mudança de runtime.
* Os três consumidores conceituais confirmados são OcorrenciaFeed, OcorrenciaMapa e a lista de ocorrências de ProfileDetailScreen.
* OcorrenciaFeed passou a estender OcorrenciaResumo e manteve localmente id_conta, observacao, latitude, longitude, distancia_km, autor_nome, autor_foto, total_forca, total_comentarios e usuario_deu_forca, com nomes e tipos preservados.
* FiltroFeed e ModoFeed permaneceram inalterados; FeedNoticias continuou consumindo OcorrenciaFeed sem modificação.
* OcorrenciaMapa passou a estender OcorrenciaResumo e manteve localmente id_conta, latitude e longitude, com nomes e tipos preservados.
* ProfileDetailScreen removeu somente a interface local duplicada Ocorrencia; OcorrenciaCardProps e o estado minhasOcorrencias passaram a usar OcorrenciaResumo.
* OcorrenciaDetalhe permaneceu local em OccurrenceDetailDrawer e OcorrenciaEdicao permaneceu local em CadastroOcorrenciaScreen.
* Nenhum tipo de cuidado, Mapbox/geocoding ou formulário foi movido; nenhum supertipo artificial foi criado.
* As alterações em Feed, Map e Profile foram exclusivamente de tipagem; lógica, JSX, callbacks, FlatList, styles e renderização permaneceram inalterados.
* GET `/ocorrencias/minhas` e todos os demais endpoints, payloads e contratos de runtime permaneceram intactos.
* Mapbox, backend, dependências e funcionalidades existentes não foram alterados.
* Nenhum RF novo, hook, service, util, constant, style compartilhado, pasta vazia, index.ts ou `.gitkeep` foi criado.
* occurrenceService ainda NÃO foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7C foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7C:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7D — occurrenceService / leituras:
* Criado `mobile/src/features/occurrences/services/occurrenceService.ts` somente para centralizar as três operações HTTP de leitura compartilhadas do domínio Occurrences.
* occurrenceService contém exclusivamente getById, getNearby e getMine.
* getById é genérico e preserva os contratos específicos locais OcorrenciaDetalhe no Drawer e OcorrenciaEdicao no Cadastro.
* getNearby é genérico e preserva os contratos específicos OcorrenciaFeed no Feed e OcorrenciaMapa no Map.
* getMine retorna `OcorrenciaResumo[]` e centraliza GET `/ocorrencias/minhas`.
* OccurrenceDetailDrawer migrou somente GET `/ocorrencias/{id}` para `getById<OcorrenciaDetalhe>`; estado, useEffect, loading, tratamento de erro, cleanup e animações permaneceram inalterados.
* CadastroOcorrenciaScreen migrou somente o GET de carregamento da edição para `getById<OcorrenciaEdicao>`; criação, edição, FormData, ImagePicker, Location, Mapbox/geocoding, prefill, validações, Alerts, JSX e styles permaneceram inalterados.
* FeedNoticias migrou somente GET `/ocorrencias/proximas` para `getNearby<OcorrenciaFeed>`.
* Os parâmetros lat, lng, raio_km e modo foram preservados no Feed, incluindo a conversão de ECO para `eco` e PROXIMIDADE para `proximidade`.
* MapScreen migrou somente GET `/ocorrencias/proximas` para `getNearby<OcorrenciaMapa>`, preservando lat, lng e raio_km sem enviar modo.
* ProfileDetailScreen migrou somente GET `/ocorrencias/minhas` para getMine; try/catch, Array.isArray, fallback, logs e independência do carregamento do Profile foram preservados.
* O import direto de api foi removido somente de ProfileDetailScreen por não possuir outro consumidor nesse arquivo; Feed, Map, Drawer e Cadastro mantiveram api para operações fora do escopo.
* POST `/ocorrencias/`, PUT `/ocorrencias/{id}` e DELETE `/ocorrencias/{id}` não foram migrados.
* Care e histórico de cuidados, força/ECO e denúncias não foram migrados e permanecem nos consumidores atuais.
* `/auth/me` não foi centralizado no occurrenceService.
* URLs, métodos, parâmetros, payloads, tipos de resposta, tratamento de erro e ordem observável das chamadas foram preservados.
* OcorrenciaDetalhe, OcorrenciaEdicao, OcorrenciaFeed e OcorrenciaMapa permaneceram em seus locais atuais.
* Mapbox, backend, dependências e funcionalidades existentes não foram alterados.
* Nenhum RF novo, hook, componente, util, constant, style compartilhado, pasta vazia, index.ts ou `.gitkeep` foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7E foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.
* `git diff --check` restrito aos arquivos da Fase 7D foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7D:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7E — occurrenceService / escritas nucleares:
* occurrenceService manteve intactos getById, getNearby e getMine e passou a conter também create, update e deleteById.
* A configuração multipart foi centralizada no mesmo service com header `Content-Type: multipart/form-data` e timeout 30000 preservados exatamente.
* create executa POST `/ocorrencias/` com o FormData recebido e a configuração multipart compartilhada.
* update executa PUT `/ocorrencias/{id}` com occurrenceId, FormData e a mesma configuração multipart.
* deleteById executa DELETE `/ocorrencias/{id}`.
* CadastroOcorrenciaScreen migrou somente POST e PUT para occurrenceService.create e occurrenceService.update.
* O objeto config local foi removido do Cadastro por não possuir outro uso; o import direto de api também foi removido por não possuir consumidor restante na Screen.
* A construção e o conteúdo do FormData, todos os campos, id_pet, foto, regras de foto remota, foto reutilizada do Pet, trim e validações permaneceram na Screen e inalterados.
* Os blocos if/else dos modos edição e criação, a ordem das operações, tratamento de erro, Alerts e navegação após sucesso permaneceram inalterados.
* ImagePicker, Location, Mapbox/geocoding e o prefill Pet → INFORMAR COMO PERDIDO permaneceram intactos.
* OccurrenceDetailDrawer migrou somente DELETE para occurrenceService.deleteById; exclusaoEmAndamento, loading, animação, onDeleted, Promise, Alerts, tratamento de erro, reset e mensagens permaneceram inalterados.
* Care e histórico de cuidados continuam diretamente no Drawer via api.
* Força/ECO e denúncias continuam diretamente no Feed e não foram migrados.
* FeedNoticias, MapScreen, ProfileDetailScreen e OcorrenciaResumo permaneceram intactos nesta subetapa.
* POST `/ocorrencias/`, PUT `/ocorrencias/{id}` e DELETE `/ocorrencias/{id}` foram centralizados sem alteração de URL, método, dados ou configuração.
* OcorrenciaDetalhe, OcorrenciaEdicao, OcorrenciaMapa e OcorrenciaFeed permaneceram em seus locais atuais.
* Mapbox, backend, dependências e funcionalidades existentes não foram alterados.
* Nenhum RF novo, hook, componente, util, constant, style compartilhado, pasta vazia, index.ts ou `.gitkeep` foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.

Smoke test Fase 7E:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7F — OccurrenceTypeSection:
* Criado `mobile/src/features/occurrences/components/form/OccurrenceTypeSection.tsx` somente para receber o corpo interativo real da seção 1 — Sobre a ocorrência.
* O componente stateless recebe `options`, `selectedType` e `onSelect`; nenhum hook, estado, regra de negócio, chamada HTTP ou integração foi introduzido.
* `TipoOcorrencia` foi movido sem alteração para `mobile/src/features/occurrences/types/occurrence.types.ts`; `OcorrenciaResumo` permaneceu intacto.
* `tiposOcorrencia` continua definido uma única vez em CadastroOcorrenciaScreen e passou somente a usar a tipagem `OccurrenceTypeOption[]` exportada pelo novo componente.
* `selecionarTipoOcorrencia` permaneceu intacto na Screen, incluindo a regra que limpa raça conhecida e raça ao selecionar um tipo diferente de PET_PERDIDO ou PET_AVISTADO.
* O `SectionHeader` e o `<View style={styles.section}>` da seção 1 permaneceram na Screen.
* JSX, ordem, textos, ícones, accessibilityRole, accessibilityState, indicador radio, estado pressed e comportamento dos três cards foram preservados no novo componente.
* Somente os styles exclusivos do corpo interativo foram movidos; o style `pressed` permaneceu na Screen para os demais consumidores e foi reproduzido localmente no novo componente.
* Nenhum outro bloco do formulário, occurrenceService, OccurrenceDetailDrawer, Feed, Map, Profile, Pets, navegação, Mapbox, backend, dependência ou contrato de API foi alterado.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7F foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7F:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7G — AnimalTypeSelector:
* Criado `mobile/src/features/occurrences/components/form/AnimalTypeSelector.tsx` somente para receber o grid visual real de seleção Cachorro, Gato e Outro da seção 2 — Sobre o animal.
* O componente stateless recebe `options`, `selectedType` e `onSelect`; nenhum hook, estado, regra de negócio, chamada HTTP ou integração foi introduzido.
* `TipoAnimal` foi movido sem alteração para `mobile/src/features/occurrences/types/occurrence.types.ts`; `TipoOcorrencia` e `OcorrenciaResumo` permaneceram intactos.
* `tiposAnimais` continua definido uma única vez em CadastroOcorrenciaScreen e passou somente a usar a tipagem `AnimalTypeOption[]` exportada pelo novo componente.
* O tipo genérico local `Opcao` foi removido após pesquisa objetiva confirmar que `tiposAnimais` era seu único consumidor restante.
* JSX, ordem, textos, ícones, accessibilityRole, accessibilityState, seleção ativa e estado pressed dos três cards foram preservados no novo componente.
* O campo "Qual animal?", seu estado e sua renderização condicional permaneceram integralmente na Screen.
* `ehPet`, raça conhecida, raça e a regra de limpeza ao marcar "Não" permaneceram integralmente na Screen.
* Sexo, Cor, Porte, Idade, `renderOpcoes`, `sexos`, `portes` e `idades` permaneceram na Screen e inalterados.
* Somente os sete styles exclusivos do grid foram movidos; `pressed` permaneceu na Screen para os demais consumidores e foi reproduzido localmente no novo componente.
* `fieldLabel`, `fieldLabelSpacing`, `otherAnimalField`, controles de input, `binaryRow`, ChoiceButton, `optionsGrid` e demais styles compartilhados permaneceram na Screen.
* Nenhuma lógica de runtime, outro bloco do formulário, OccurrenceTypeSection, occurrenceService, OccurrenceDetailDrawer, Feed, Map, Profile, Pets, navegação, Mapbox, backend, dependência ou contrato de API foi alterado.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7G foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7G:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7H — ChoiceButton:
* Criado `mobile/src/features/occurrences/components/form/ChoiceButton.tsx` somente para receber o componente local reutilizado pelos grupos de raça, saúde crítica e deficiência.
* O componente stateless preserva exatamente as props `label`, `active`, `onPress` e `danger?`, incluindo `danger = false`.
* Os seis usos reais de ChoiceButton permaneceram funcionalmente idênticos na CadastroOcorrenciaScreen: dois em raça, dois em saúde crítica e dois em deficiência.
* accessibilityRole, accessibilityState, onPress, indicador Ionicons, nome `checkmark-circle`, tamanho, cores e comportamento danger foram preservados.
* O callback de raça preserva `setRacaConhecida(false)` e `setRaca("")`.
* O callback de saúde preserva `setSaudeCritica(false)` e `setProblemasSelecionados([])`; `danger={saudeCritica}` permanece somente no uso preexistente.
* O callback de deficiência preserva `setDeficiencia(false)` e `setDeficienciasSelecionadas([])`.
* Somente os seis styles exclusivos do ChoiceButton foram movidos; `pressed` permaneceu na Screen para os demais consumidores e foi reproduzido localmente no novo componente.
* `binaryRow` e seus três usos permaneceram na Screen.
* `renderOpcoes`, `renderMultiOpcoes` e `alternarItem` permaneceram na Screen e inalterados.
* AnimalTypeSelector, OccurrenceTypeSection, occurrence.types, occurrenceService e OccurrenceDetailDrawer permaneceram intactos.
* Nenhuma lógica de runtime, FormData, criação, edição, exclusão, prefill de Pet, Location, ImagePicker, Mapbox/geocoding, Feed, Map, Profile, Pets, navegação, backend, endpoint, payload, Alert ou validação foi alterada.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7H foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7H:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7I — SelectionChip:
* Criado `mobile/src/features/occurrences/components/form/SelectionChip.tsx` somente para receber o elemento visual/interativo compartilhado por `renderOpcoes` e `renderMultiOpcoes`.
* O componente stateless recebe exclusivamente `label`, `active`, `mode` e `onPress`; `mode` aceita somente `single` ou `multiple`.
* O modo single preserva o Pressable sem checkCircle, accessibilityRole `radio`, accessibilityState `selected`, estado ativo e pressed.
* O modo multiple preserva accessibilityRole `checkbox`, accessibilityState `checked`, checkCircle, checkCircle ativo e Ionicons `checkmark` com tamanho 13 e cor `theme.colors.surface`.
* `renderOpcoes` permaneceu na Screen com assinatura, `optionsGrid`, map, cálculo de ativo e callback `onChange(opcao)` preservados; somente o Pressable interno passou a usar SelectionChip em modo single.
* `renderMultiOpcoes` permaneceu na Screen com assinatura, `optionsGrid`, map, cálculo de ativo e callback `alternarItem(opcao, setSelecionados)` preservados; somente o Pressable interno passou a usar SelectionChip em modo multiple.
* `alternarItem` permaneceu na Screen e inalterado.
* Sexo, Porte, Idade e seus três consumidores de `renderOpcoes` permaneceram intactos.
* Saúde crítica, Deficiência e seus dois consumidores de `renderMultiOpcoes` permaneceram intactos.
* Somente os seis styles exclusivos do chip foram movidos; `optionsGrid` e `pressed` permaneceram na Screen, e `pressed` foi reproduzido localmente no novo componente.
* ChoiceButton, AnimalTypeSelector, OccurrenceTypeSection, occurrence.types, occurrenceService e OccurrenceDetailDrawer permaneceram intactos.
* Nenhuma lógica de seleção, runtime, FormData, criação, edição, exclusão, prefill de Pet, Location, ImagePicker, Mapbox/geocoding, Feed, Map, Profile, Pets, navegação, backend, endpoint, payload, Alert ou validação foi alterada.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7I foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7I:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7J — SelectionChipGroup:
* Criado `mobile/src/features/occurrences/components/form/SelectionChipGroup.tsx` somente para renderizar grupos de opções por meio do SelectionChip já validado.
* O componente stateless usa union discriminada por `mode`: single exige `selectedValue` e multiple exige `selectedValues`; ambos recebem `options` e `onSelect(value)` sem props opcionais inválidas.
* SelectionChip foi reutilizado diretamente e permaneceu intacto.
* O style `optionsGrid` foi movido sem alteração para SelectionChipGroup e não permanece na Screen.
* Os helpers puramente renderizadores `renderOpcoes` e `renderMultiOpcoes` foram removidos da Screen.
* Sexo, Porte e Idade passaram a consumir diretamente três SelectionChipGroup em modo single, preservando arrays, estados, setters, textos, ordem e seleção única.
* Problemas de saúde e deficiências passaram a consumir diretamente dois SelectionChipGroup em modo multiple, preservando arrays, estados, callbacks e seleção múltipla.
* `alternarItem` permaneceu intacto na Screen e continua responsável por incluir ou remover o item nos dois callbacks multiple.
* ChoiceButton, AnimalTypeSelector, OccurrenceTypeSection, SelectionChip, occurrence.types, occurrenceService e OccurrenceDetailDrawer permaneceram intactos.
* Nenhuma regra de seleção, lógica de runtime, FormData, criação, edição, exclusão, prefill de Pet, Location, ImagePicker, Mapbox/geocoding, Feed, Map, Profile, Pets, navegação, backend, endpoint, payload, Alert ou validação foi alterada.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7J foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7J:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7K — estilos compartilhados do formulário:
* Criado `mobile/src/features/occurrences/components/form/occurrenceForm.styles.ts` somente para centralizar o núcleo de styles com reuso comprovado no formulário de ocorrência.
* `occurrenceFormSharedStyles` exporta um objeto `as const`, sem StyleSheet.create e sem `any`, contendo exatamente fieldLabel, fieldLabelSpacing, inputContainer, inputIcon, input e binaryRow.
* Os valores das seis definições foram preservados integralmente.
* CadastroOcorrenciaScreen incorpora `...occurrenceFormSharedStyles` no StyleSheet.create existente; não foi criado segundo StyleSheet.
* Todas as referências JSX continuam usando `styles.fieldLabel`, `styles.fieldLabelSpacing`, `styles.inputContainer`, `styles.inputIcon`, `styles.input` e `styles.binaryRow`; nenhum JSX foi alterado.
* `otherAnimalField` e `optionalText` permaneceram locais na Screen para futura avaliação junto ao AnimalSection.
* `addressInput`, demais styles específicos de Localização, `conditionalBox`, `conditionalTitle`, `sectionDivider` e demais styles específicos de Saúde permaneceram locais.
* OccurrenceTypeSection, AnimalTypeSelector, ChoiceButton, SelectionChip e SelectionChipGroup permaneceram intactos.
* Toda a lógica do formulário, incluindo estados, efeitos, callbacks, handlers, setters, validações e FormData, permaneceu intacta.
* occurrence.types, occurrenceService, OccurrenceDetailDrawer, criação, edição, exclusão, prefill de Pet, foto, ImagePicker, Location, Mapbox/geocoding, Feed, Map, Profile, Pets, navegação, backend, endpoints, payloads e Alerts permaneceram intactos.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7K foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7K:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7L — OccurrenceFormSection:
* Criado `mobile/src/features/occurrences/components/form/OccurrenceFormSection.tsx` somente para receber o shell visual compartilhado das nove seções do formulário.
* O componente stateless recebe exclusivamente `number`, `title`, `subtitle` e `children`.
* Os nove wrappers `<View style={styles.section}>` e seus nove SectionHeader foram substituídos por exatamente nove usos de OccurrenceFormSection.
* A numeração de 1 a 9, títulos e subtítulos das seções foram preservados integralmente.
* O helper local SectionHeader foi removido da CadastroOcorrenciaScreen e seu JSX foi preservado dentro de OccurrenceFormSection.
* Os sete styles exclusivos section, sectionHeader, sectionNumber, sectionNumberText, sectionHeaderContent, sectionTitle e sectionSubtitle foram movidos com valores idênticos.
* O conteúdo interno das nove seções permaneceu funcionalmente intacto na Screen; AnimalSection, HealthSection e DisabilitySection não foram criados.
* `occurrenceFormSharedStyles` e `occurrenceForm.styles.ts` permaneceram intactos.
* OccurrenceTypeSection, AnimalTypeSelector, ChoiceButton, SelectionChip e SelectionChipGroup permaneceram intactos.
* Toda a lógica do formulário, incluindo estados, efeitos, callbacks, refs, handlers, setters, validações e FormData, permaneceu intacta.
* occurrence.types, occurrenceService, OccurrenceDetailDrawer, criação, edição, exclusão, prefill de Pet, Location, ImagePicker, Mapbox/geocoding, Feed, Map, Profile, Pets, navegação, backend, endpoints, payloads e Alerts permaneceram intactos.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7L foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7L:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7M — AnimalSection:
* Criado `mobile/src/features/occurrences/components/form/AnimalSection.tsx` somente para receber o conteúdo controlado da seção 2 — Sobre o animal.
* O componente é stateless e controlado por props explícitas de valores e callbacks; nenhum hook, estado, regra de carregamento, chamada HTTP ou integração foi introduzido.
* Os estados tipoAnimal, tipoAnimalOutro, sexo, cor, porte, idade, racaConhecida e raca permaneceram integralmente na CadastroOcorrenciaScreen.
* O cálculo memoizado de ehPet permaneceu na Screen e somente seu resultado é fornecido ao AnimalSection.
* A regra de limpar raca ao selecionar raça não conhecida permaneceu na Screen por meio do callback simples selecionarRacaConhecida, sem useCallback e sem alteração de semântica.
* As constantes exclusivas tiposAnimais, sexos, portes e idades foram movidas para AnimalSection com dados, textos, ordem e ícones preservados.
* Os styles exclusivos otherAnimalField e optionalText foram movidos com valores idênticos; occurrenceFormSharedStyles passou a ser reutilizado pelo novo componente e permaneceu intacto.
* AnimalTypeSelector, ChoiceButton e SelectionChipGroup foram reutilizados diretamente e permaneceram intactos.
* O shell OccurrenceFormSection número 2, com título e subtítulo existentes, permaneceu na CadastroOcorrenciaScreen; somente seu conteúdo interno passou a renderizar AnimalSection.
* O prefill originado de Pet e o carregamento do modo edição continuam escrevendo nos mesmos estados da Screen e permaneceram intactos.
* Saúde, Deficiência, Urgência, Cuidados, Foto, Observação, Revisão, Localização e as demais seções do formulário permaneceram intactas.
* occurrenceService, OccurrenceDetailDrawer, FormData, criação, edição, exclusão, Location, ImagePicker, Mapbox/geocoding, Feed, Map, Profile, Pets, navegação, backend, endpoints, payloads e Alerts permaneceram intactos.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7M foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7M:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7N — HealthSection:
* Criado `mobile/src/features/occurrences/components/form/HealthSection.tsx` somente para receber a subseção controlada Saúde crítica da seção 4 — Condições de saúde.
* O componente é stateless e controlado pelas props saudeCritica, onSaudeCriticaChange, problemasSelecionados e onProblemaSelect; nenhum hook, estado, regra de carregamento, chamada HTTP ou integração foi introduzido.
* Os estados saudeCritica e problemasSelecionados permaneceram integralmente na CadastroOcorrenciaScreen.
* O callback simples selecionarSaudeCritica foi criado na Screen para preservar a regra existente de limpar problemasSelecionados somente quando Saúde crítica recebe false, sem useCallback e sem alteração de semântica.
* A constante exclusiva problemasSaude foi movida para HealthSection com textos, ordem, capitalização e pontuação preservados.
* ChoiceButton e SelectionChipGroup foram reutilizados diretamente e permaneceram intactos.
* Criado o export separado occurrenceConditionStyles em occurrenceForm.styles.ts, contendo exclusivamente conditionalBox e conditionalTitle com seus valores anteriores.
* O objeto occurrenceFormSharedStyles original permaneceu intacto com os mesmos seis styles; Screen e HealthSection passaram a incorporar occurrenceConditionStyles separadamente.
* As definições locais conditionalBox e conditionalTitle foram removidas da Screen, enquanto seus usos na subseção Deficiência permaneceram inalterados.
* sectionDivider permaneceu definido e renderizado na CadastroOcorrenciaScreen entre HealthSection e Deficiência.
* Deficiência, seus estados, constante, ChoiceButtons, SelectionChipGroup e comportamento permaneceram integralmente na Screen.
* O carregamento do modo edição, o reset e a construção do FormData de Saúde permaneceram nos mesmos pontos da Screen e inalterados.
* AnimalSection, AnimalTypeSelector, OccurrenceFormSection, OccurrenceTypeSection, ChoiceButton, SelectionChip e SelectionChipGroup permaneceram intactos.
* As demais seções do formulário, occurrenceService, OccurrenceDetailDrawer, criação, edição, exclusão, prefill de Pet, Location, ImagePicker, Mapbox/geocoding, Feed, Map, Profile, Pets, navegação, backend, endpoints, payloads e Alerts permaneceram intactos.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7N foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7N:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7O — DisabilitySection:
* Criado `mobile/src/features/occurrences/components/form/DisabilitySection.tsx` somente para receber a subseção controlada Deficiência da seção 4 — Condições de saúde.
* O componente é stateless e controlado pelas props deficiencia, onDeficienciaChange, deficienciasSelecionadas e onDeficienciaSelect; nenhum hook, estado, regra de carregamento, chamada HTTP ou integração foi introduzido.
* Os estados deficiencia e deficienciasSelecionadas permaneceram integralmente na CadastroOcorrenciaScreen.
* O callback simples selecionarDeficiencia foi criado na Screen para preservar a regra existente de limpar deficienciasSelecionadas somente quando Deficiência recebe false, sem useCallback e sem alteração de semântica.
* A constante exclusiva deficiencias foi movida para DisabilitySection com textos, ordem, acentuação e capitalização preservados.
* ChoiceButton e SelectionChipGroup foram reutilizados diretamente e permaneceram intactos.
* occurrenceFormSharedStyles e occurrenceConditionStyles foram reutilizados pelo novo componente e occurrenceForm.styles.ts permaneceu intacto.
* Os imports diretos agora mortos de ChoiceButton, SelectionChipGroup e occurrenceConditionStyles, além do spread de occurrenceConditionStyles, foram removidos somente da Screen após pesquisa objetiva de usos.
* sectionDivider permaneceu definido e renderizado na CadastroOcorrenciaScreen com valores intactos.
* A sequência HealthSection, sectionDivider e DisabilitySection foi preservada dentro de OccurrenceFormSection número 4.
* HealthSection permaneceu intacto.
* O carregamento do modo edição, o reset, a construção do FormData e o resumo de Revisão de Deficiência permaneceram nos mesmos pontos da Screen e inalterados.
* AnimalSection, AnimalTypeSelector, OccurrenceFormSection, OccurrenceTypeSection, ChoiceButton, SelectionChip, SelectionChipGroup e occurrenceForm.styles.ts permaneceram intactos.
* As demais seções do formulário, occurrenceService, OccurrenceDetailDrawer, criação, edição, exclusão, prefill de Pet, Location, ImagePicker, Mapbox/geocoding, Feed, Map, Profile, Pets, navegação, backend, endpoints, payloads e Alerts permaneceram intactos.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7O foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7O:
APROVADO MANUALMENTE PELO USUÁRIO.

Subetapa Fase 7P — InitialCareSection:
* Criado `mobile/src/features/occurrences/components/form/InitialCareSection.tsx` somente para receber o conteúdo controlado da seção 6 — Cuidados iniciais.
* O componente é stateless e controlado pelas props aguaRegistrada, comidaRegistrada, aguaTexto, comidaTexto, onAguaPress e onComidaPress; nenhum hook, Date, estado, regra de toggle, FormData ou integração foi introduzido.
* Os estados aguaRegistrada, comidaRegistrada e cuidadosIniciaisAlterados permaneceram integralmente na CadastroOcorrenciaScreen.
* A função registrarCuidado permaneceu intacta na Screen, incluindo Date, marcação de alteração e regras de toggle de água e comida.
* formatarHora, formatarDataCurta e cuidadosFormatados permaneceram intactos na Screen para uso da Revisão e do payload existente.
* Foram criados na Screen somente aguaRegistradaTexto e comidaRegistradaTexto para fornecer ao componente os textos já formatados, com fallback "Não registrada" preservado.
* CareButton e CareInfo foram movidos integralmente para InitialCareSection, preservando props, accessibilityState, ícones, tamanhos, textos e comportamento visual.
* O conteúdo interno da seção Cuidados iniciais passou a renderizar exatamente um InitialCareSection; o shell OccurrenceFormSection número 6 permaneceu na Screen.
* Os styles careRow, careButton, careButtonActive, careIcon, careIconActive, careButtonText, careButtonTextActive, careCheck, caregiverCard, careInfoRow, careInfo, careInfoHeader, careInfoLabel, careInfoValue, careHint e careHintText foram movidos com valores idênticos.
* O style local pressed com opacity 0.82 foi adicionado ao novo componente; o styles.pressed da Screen permaneceu intacto para seus demais consumidores.
* Ionicons, MaterialCommunityIcons, Pressable, Text, View e theme permaneceram importados na Screen por possuírem outros usos reais.
* Reset, carregamento de edição, FormData, Revisão e cuidadosFormatados permaneceram inalterados.
* HealthSection, DisabilitySection, AnimalSection e as demais seções do formulário permaneceram intactas.
* occurrenceService, OccurrenceDetailDrawer, criação, edição, exclusão, prefill de Pet, Location, ImagePicker, Mapbox/geocoding, Feed, Map, Profile, Pets, navegação, backend, endpoints, payloads e Alerts permaneceram intactos.
* Nenhum RF novo ou abstração adicional foi criado.
* `npx tsc --noEmit` foi executado uma vez e concluído com 0 erros.
* `git diff --check` restrito aos arquivos da Fase 7P foi concluído sem erros; os consolidados preexistentes permaneceram fora do escopo.

Smoke test Fase 7P:
PENDENTE DE VALIDAÇÃO MANUAL.

======================================================================
ETAPAS CONCLUÍDAS
=================

[X] Código atual analisado antes da refatoração
[X] Plano Mestre lido
[X] AGENTS.md considerado
[X] Nenhum RF novo iniciado
[X] TypeScript validado antes da refatoração
[X] Aplicativo Android compilou e iniciou
[X] Working tree inicial confirmada limpa
[X] Commit funcional de baseline identificado
[X] Branch específica de refatoração criada
[X] Quatro blocos de testes funcionais manuais aprovados
[X] Nenhum bug funcional atual reproduzido durante a baseline
[X] TypeScript validado novamente no encerramento da Fase 0
[X] Estado Git e diff revisados antes do fechamento
[X] Fase 0 concluída sem refatoração estrutural
[X] Fase 3 — Auth concluída após validação técnica e smoke tests manuais
[X] Fase 4A — FeedBannerCarousel movido para o domínio Feed
[X] Smoke test manual da Fase 4A aprovado pelo usuário
[X] Fase 4B — FeedNoticias movida para o domínio Feed
[X] Smoke test manual da Fase 4B aprovado pelo usuário
[X] Fase 4C — OccurrenceCard extraído para `features/feed/components/`
[X] Smoke test manual da Fase 4C aprovado pelo usuário
[X] Fase 4D — FeedControls extraído para `features/feed/components/`
[X] Smoke test manual da Fase 4D aprovado pelo usuário
[X] Fase 4E — feedButtonPressedStyle movido para `features/feed/styles/feed.styles.ts`
[X] Smoke test manual da Fase 4E aprovado pelo usuário
[X] Organização interna atual do Feed avaliada
[X] Fase 4F — checkpoint técnico de consolidação de 4D + 4E
[X] Fase 4G — fechamento formal da Fase 4 — Feed
[X] Fase 4 — Feed concluída
[X] Fase 5A — ProfileQuickMenu movido para `features/profile/components/`
[X] Smoke test manual da Fase 5A aprovado pelo usuário
[X] Fase 5B — ProfileDetailScreen movido para `features/profile/components/`
[X] Smoke test manual da Fase 5B aprovado pelo usuário
[X] Fase 5C — operações HTTP próprias de Profile extraídas para profileService
[X] Smoke test manual da Fase 5C aprovado pelo usuário
[X] Organização interna de Profile avaliada
[X] Fase 5D — avaliação final e fechamento formal da Fase 5 — Profile
[X] Fase 5 — Profile concluída
[X] Fase 6A — RegistrarPet movido para `features/pets/components/`
[X] Smoke test manual da Fase 6A aprovado pelo usuário
[X] Fase 6B — Pet compartilhado e operações HTTP de Pets extraídos para pet.types e petService
[X] Smoke test manual da Fase 6B aprovado pelo usuário
[X] Fase 6C — PetCard extraído para `features/pets/components/`
[X] Smoke test manual da Fase 6C aprovado pelo usuário
[X] Fase 6D — PetDetailModal extraído para `features/pets/components/`
[X] Smoke test manual da Fase 6D aprovado pelo usuário
[X] Fase 6E — PetFormModal extraído para `features/pets/components/`
[X] Smoke test manual da Fase 6E aprovado pelo usuário
[X] Fase 6F — RegistrarPet renomeado semanticamente para PetsTab
[X] Smoke test manual da Fase 6F aprovado pelo usuário
[X] Fase 6G — fechamento formal da Fase 6 — Pets
[X] Fase 6 — Pets concluída
[X] Fase 7A — OccurrenceDetailDrawer movido para `features/occurrences/components/`
[X] Validação técnica da Fase 7A concluída
[X] Smoke test manual da Fase 7A aprovado pelo usuário
[X] Fase 7B — CadastroOcorrenciaScreen movida para `features/occurrences/screens/`
[X] Validação técnica da Fase 7B concluída
[X] Smoke test manual da Fase 7B aprovado pelo usuário
[X] Fase 7C — OcorrenciaResumo extraído para `features/occurrences/types/occurrence.types.ts`
[X] Validação técnica da Fase 7C concluída
[X] Smoke test manual da Fase 7C aprovado pelo usuário
[X] Fase 7D — operações de leitura centralizadas em `features/occurrences/services/occurrenceService.ts`
[X] Validação técnica da Fase 7D concluída
[X] Smoke test manual da Fase 7D aprovado pelo usuário
[X] Fase 7E — operações de escrita nucleares centralizadas em `features/occurrences/services/occurrenceService.ts`
[X] Validação técnica da Fase 7E concluída
[X] Smoke test manual da Fase 7E aprovado pelo usuário
[X] Fase 7F — OccurrenceTypeSection extraído para `features/occurrences/components/form/`
[X] TipoOcorrencia compartilhado via `features/occurrences/types/occurrence.types.ts`
[X] Validação técnica da Fase 7F concluída
[X] Smoke test manual da Fase 7F aprovado pelo usuário
[X] Fase 7G — AnimalTypeSelector extraído para `features/occurrences/components/form/`
[X] TipoAnimal compartilhado via `features/occurrences/types/occurrence.types.ts`
[X] Validação técnica da Fase 7G concluída
[X] Smoke test manual da Fase 7G aprovado pelo usuário
[X] Fase 7H — ChoiceButton extraído para `features/occurrences/components/form/`
[X] Validação técnica da Fase 7H concluída
[X] Smoke test manual da Fase 7H aprovado pelo usuário
[X] Fase 7I — SelectionChip extraído para `features/occurrences/components/form/`
[X] Validação técnica da Fase 7I concluída
[X] Smoke test manual da Fase 7I aprovado pelo usuário
[X] Fase 7J — SelectionChipGroup extraído para `features/occurrences/components/form/`
[X] Validação técnica da Fase 7J concluída
[X] Smoke test manual da Fase 7J aprovado pelo usuário
[X] Fase 7K — estilos compartilhados centralizados em `features/occurrences/components/form/occurrenceForm.styles.ts`
[X] Validação técnica da Fase 7K concluída
[X] Smoke test manual da Fase 7K aprovado pelo usuário
[X] Fase 7L — OccurrenceFormSection extraído para `features/occurrences/components/form/`
[X] Validação técnica da Fase 7L concluída
[X] Smoke test manual da Fase 7L aprovado pelo usuário
[X] Fase 7M — AnimalSection extraído para `features/occurrences/components/form/`
[X] Validação técnica da Fase 7M concluída
[X] Smoke test manual da Fase 7M aprovado pelo usuário
[X] Fase 7N — HealthSection extraído para `features/occurrences/components/form/`
[X] Validação técnica da Fase 7N concluída
[X] Smoke test manual da Fase 7N aprovado pelo usuário
[X] Fase 7O — DisabilitySection extraído para `features/occurrences/components/form/`
[X] Validação técnica da Fase 7O concluída
[X] Smoke test manual da Fase 7O aprovado pelo usuário
[X] Fase 7P — InitialCareSection extraído para `features/occurrences/components/form/`
[X] Validação técnica da Fase 7P concluída
[ ] Smoke test manual da Fase 7P

======================================================================
VALIDAÇÕES DA BASELINE
======================

TypeScript:

Comando:
cd mobile
npx tsc --noEmit

Resultado:
SUCESSO — 0 erros TypeScript.

Validação final para encerramento da Fase 0:

Comando:
cd mobile
npx tsc --noEmit

Resultado:
SUCESSO — 0 erros TypeScript em 16/08/2026.

Android:

Comando:
cd mobile
npx expo run:android

Resultado:
SUCESSO — aplicativo compilou e abriu normalmente.

======================================================================
TESTES FUNCIONAIS MÍNIMOS
=========================

[X] aplicativo compila sem erro TypeScript
[X] aplicativo inicia no Android

BLOCO 1 — AUTENTICAÇÃO + MAPA

[X] Login
[X] Mapa abre
[X] Localização carrega
[X] Ocorrências aparecem
[X] Abrir detalhes de ocorrência
[X] Voltar sem erro

Resultado do bloco:
APROVADO.

BLOCO 2 — FEED

[X] Abrir Feed
[X] Proximidade
[X] Eco
[X] Busca
[X] Filtros
[X] Refresh
[X] Abrir ocorrência pelo Feed
[X] Voltar sem erro

Resultado do bloco:
APROVADO.

BLOCO 3 — PERFIL / PETS

[X] Abrir perfil
[X] Visualizar dados
[X] Visualizar pets
[X] Testar raio de pesquisa
[X] Editar perfil
[X] Cadastrar pet
[X] Editar pet
[X] Voltar/navegar sem erro

Resultado do bloco:
APROVADO.

BLOCO 4 — OCORRÊNCIA / CUIDADOS

[X] Cadastrar ocorrência
[X] Upload/foto da ocorrência
[X] Abrir ocorrência cadastrada
[X] Editar ocorrência
[X] Atualizar água
[X] Atualizar comida
[X] Voltar/navegar sem erro

Resultado do bloco:
APROVADO.

Resultado geral:
Os quatro blocos funcionais da baseline foram aprovados pelo usuário.

======================================================================
PROBLEMAS PREEXISTENTES
=======================

Resultado dos testes funcionais:
Nenhum bug funcional preexistente foi reproduzido durante a baseline.

Registros históricos de logcat:
Os crashes encontrados anteriormente no buffer do logcat permanecem registrados apenas como eventos históricos. Eles não foram reproduzidos durante os testes completos da baseline e não são classificados como bugs atuais.

======================================================================
ALTERAÇÕES DE CÓDIGO REALIZADAS PELA REFATORAÇÃO
================================================

Nenhuma.

Nenhuma refatoração estrutural foi realizada durante a Fase 0.

Nenhum arquivo de aplicação, backend, dependência, contrato de API ou configuração funcional foi alterado durante a Fase 0.

O único novo arquivo de acompanhamento deverá ser:
`STATUS_REFATORACAO_PETRADAR.txt`

Subetapa Fase 1A — Tipos de navegação:
* Criado `mobile/src/navigation/navigation.types.ts`.
* Movidos `AuthStackParamList`, `PetOcorrenciaPrefill` e `AppTabParamList` sem alteração de contrato.
* Atualizados apenas os imports de tipagem no App.tsx e nas Screens dependentes.
* Nenhum navegador foi movido ou criado.

Subetapa Fase 1B — AuthNavigator:
* Criado `mobile/src/navigation/AuthNavigator.tsx`.
* Movidos somente AuthStack, AuthNavigator e imports exclusivos das telas de autenticação.
* Preservadas as rotas Login, TipoCadastro, CadastroUser, CadastroONG e EsqueceuSenha na mesma ordem.
* Preservado `screenOptions={{ headerShown: false }}`.
* AppNavigator, Routes, providers e decisão de autenticação permanecem no App.tsx.

Subetapa Fase 1C — AppNavigator:
* Criado `mobile/src/navigation/AppNavigator.tsx`.
* Movidos somente AppTabs, AppNavigator e imports exclusivos das telas registradas.
* Movidos SOSScreen e ProfileScreen temporários por serem dependências exclusivas do AppNavigator, sem alteração de JSX.
* Preservadas as rotas Mapa, Feed, SOS, Perfil e CadastroOcorrencia na mesma ordem.
* Preservados `tabBar={() => null}` e os screenOptions existentes.
* Routes, providers, AppStack e decisão de autenticação permanecem no App.tsx.

Subetapa Fase 1D — RootNavigator:
* Criado `mobile/src/navigation/RootNavigator.tsx`.
* Movidos somente useAuthStore, AuthNavigator, AppNavigator e a decisão autenticado/não autenticado.
* Preservada integralmente a decisão baseada em `isAuthenticated`.
* NavigationContainer, providers, StatusBar e função App permanecem no App.tsx.
* AuthNavigator, AppNavigator e navigation.types.ts não foram alterados.

======================================================================
DECISÕES TOMADAS
================

* Refatoração incremental.
* Não implementar RF novo durante o Plano Mestre.
* Não reescrever o projeto.
* Não substituir Mapbox.
* Preservar endpoints e contratos de API.
* Preservar comportamento visual e funcional.
* Trabalhar em passos pequenos e verificáveis.
* Cada alteração deverá passar por validação antes do próximo passo.
* A branch `refactor/frontend-modularization` será utilizada para a refatoração.
* A Fase 1 será executada incrementalmente, uma subetapa por vez.
* Os tipos de navegação passam a ser importados de `src/navigation/navigation.types.ts`.
* AuthNavigator passa a residir em `src/navigation/AuthNavigator.tsx`.
* AppNavigator passa a residir em `src/navigation/AppNavigator.tsx`.
* RootNavigator passa a residir em `src/navigation/RootNavigator.tsx`.
* A decisão autenticado/não autenticado deixa o App.tsx e passa a residir no RootNavigator.
* NavigationContainer e providers permanecem no App.tsx nesta subetapa.
* A estrutura modular base foi definida conceitualmente sem criar diretórios vazios ou mover arquivos.
* Diretórios de domínio serão criados progressivamente ao receberem o primeiro arquivo real.
* `features/auth/screens/` foi criado somente ao receber TipoCadastroScreen.tsx como primeiro arquivo real.
* O nome e o `any` preexistente de TipoCadastroScreen foram preservados na Fase 3A.
* EsqueceuSenhaScreen foi movida sem renomeação na Fase 3B.
* O import relativo de api.ts foi ajustado sem mover ou alterar o módulo de API.
* LoginScreen foi movida sem renomeação na Fase 3C.
* Os caminhos relativos de api.ts, useAuthStore.ts e logo.png foram ajustados sem mover ou alterar esses arquivos.
* CadastroUserScreen foi movida sem renomear o arquivo ou o componente interno CadastroUsuarioScreen na Fase 3D.
* O import relativo de api.ts foi ajustado sem mover ou alterar o módulo; o uso direto de expo-location foi preservado.
* CadastroONGScreen foi movida sem renomeação na Fase 3E, concluindo a movimentação física das cinco telas Auth.
* O import relativo de api.ts foi ajustado sem mover o módulo; expo-location, expo-image-picker e o comprovante foram preservados sem organização interna adicional.
* authService foi criado porque quatro Screens repetiam acesso direto ao cliente HTTP para cinco operações do domínio Auth.
* useLogin, useRegister e demais hooks de Auth não foram criados porque não há justificativa suficiente neste momento.
* auth.types.ts não foi criado separadamente porque os novos tipos possuem uso específico dentro de authService.ts e não há reuso real entre arquivos.
* useAuthStore permanece no local atual porque possui consumidores transversais e sua movimentação está fora do escopo desta subetapa.
* Renomeações de Screens continuam adiadas.
* api.ts permanece em `mobile/src/services/` até a Fase 10 — Infrastructure.
* `features/feed/components/` foi criado somente ao receber FeedBannerCarousel.tsx como primeiro código real do domínio Feed.
* FeedBannerCarousel foi movido antes de FeedNoticias por possuir superfície de dependências menor e um único consumidor executável.
* `features/feed/screens/` foi criado somente ao receber FeedNoticias.tsx como código real.
* FeedNoticias foi movida primeiro sem organização interna para isolar e validar a mudança física antes de qualquer extração adicional.
* feedService, hooks, types e novos componentes não foram criados nesta subetapa porque as responsabilidades atuais continuam misturadas e sua separação exige análise e validação próprias.
* O smoke test manual da Fase 4B foi aprovado antes do início da primeira extração interna do Feed.
* OccurrenceCard foi escolhido como primeira extração interna por já possuir fronteira explícita baseada em dados e callbacks.
* OcorrenciaFeed foi movido para `features/feed/types/feed.types.ts` somente após passar a ter dois consumidores reais.
* normalizarTexto e ehUrgente residem em `features/feed/utils/feed.utils.ts` porque ambos passaram a possuir uso real na Screen e no card.
* Nenhum service ou hook foi criado na Fase 4C; as operações HTTP continuam separadas apenas pela orquestração atual da Screen até análise específica posterior.
* A avaliação da organização interna do Feed continuou somente após a aprovação do smoke test manual da Fase 4C.
* O smoke test manual da Fase 4C foi aprovado antes do início da extração de FeedControls.
* FeedControls foi escolhido como a próxima extração interna por possuir uma fronteira visual coesa de busca, modos e filtros, mantendo estados e comportamento na Screen.
* FiltroFeed e ModoFeed foram movidos para feed.types.ts somente após passarem a possuir dois consumidores reais.
* FiltroConfig e FILTROS permanecem internos de FeedControls porque não possuem reuso externo.
* feedModeCarouselRef foi internalizado em FeedControls após a pesquisa confirmar somente declaração e uso no ScrollView transferido.
* feedButtonPressedStyle deixou de pertencer a OccurrenceCard e passou a residir em `features/feed/styles/feed.styles.ts`, com uma única definição compartilhada pelos três consumidores reais do domínio Feed.
* A avaliação restante da organização interna do Feed continuou somente após a aprovação do smoke test manual da Fase 4D.
* A organização interna atual do Feed foi avaliada no código real; após a aprovação manual da Fase 4E, não há outra extração interna atualmente justificada.
* As Fases 4D e 4E são consolidadas em um único commit técnico porque foram implementadas sequencialmente no mesmo working tree antes de qualquer commit intermediário, sem reconstrução artificial de estado.
* A Fase 4 foi encerrada sem criar novas abstrações porque a análise do código real não demonstrou benefício arquitetural suficiente para extrações adicionais no escopo atual do Feed.
* AppNavigationDrawer permanece candidato a Shared na Fase 11; OccurrenceDetailDrawer permanece reservado ao domínio Occurrences.
* ProfileQuickMenu foi escolhido como primeira subetapa de Profile por possuir fronteira clara, contrato por props e somente dois consumidores executáveis.
* ProfileDetailScreen foi movido sem renomeação para isolar e validar primeiro a mudança física; eventual renomeação para Drawer permanece adiada.
* A dependência direta de ProfileDetailScreen com RegistrarPet foi preservada temporariamente e será tratada somente na Fase 6 — Pets.
* profileService concentra somente as quatro operações `/auth/me`; `/ocorrencias/minhas` permanece fora do service de Profile por pertencer ao domínio Occurrences.
* profile.types.ts contém somente ProfileUpdateResult, UserProfile e ProfileUpdatePayload, todos com reuso concreto.
* Avatar, TabBar, InfoRow, PetSwitch e OcorrenciaCard permanecem internos a ProfileDetailScreen porque não possuem consumidores externos e nenhuma extração visual adicional demonstrou ganho arquitetural real.
* ProfileDetailScreen mantém o nome atual durante a Fase 5; eventual renomeação para Drawer será reavaliada somente em Cleanup se houver benefício real.
* RegistrarPet permanece como dependência temporária de Profile até a Fase 6 — Pets.
* `/ocorrencias/minhas` permanece diretamente em ProfileDetailScreen até a Fase 7 — Occurrences.
* expo-image-picker permanece diretamente em ProfileDetailScreen até a Fase 10 — Infrastructure.
* As pendências preexistentes de App.tsx permanecem reservadas para fases posteriores, especialmente Cleanup/App.
* RegistrarPet foi movido primeiro para `features/pets/components/` com nome e implementação preservados; após a aprovação do smoke 6A, a Fase 6B ficou limitada à fronteira real de tipo e service.
* A ausência de edição e exclusão de pet é comportamento atual preexistente e critério não aplicável nesta etapa; nenhum RF ou endpoint será criado durante a refatoração.
* expo-image-picker, FormData, tipos, handlers, JSX e StyleSheet de Pets permanecem juntos em RegistrarPet durante a movimentação estrutural inicial.
* Pet foi extraído para `features/pets/types/pet.types.ts` somente após adquirir dois consumidores reais: RegistrarPet e petService.
* TipoEspecie, FotoSelecionada e DetailRowProps permanecem internos porque não possuem reuso concreto fora de RegistrarPet.
* petService centraliza somente getMyPets e createPet; FormData, ImagePicker, validação, UI e integração com CadastroOcorrencia permanecem no componente.
* PetCard foi extraído por possuir fronteira visual própria e recebe somente Pet e onPress; estado, formulários, detalhes, API e navegação permanecem no pai.
* buttonPressed permanece também em RegistrarPet porque Adicionar pet e INFORMAR COMO PERDIDO continuam sendo consumidores reais do mesmo efeito visual.
* PetDetailModal foi extraído como componente apresentacional com Pet, onClose e onReportLost; estado selecionado, informarComoPerdido e navigation permanecem em RegistrarPet.
* DetailRow permanece interno a PetDetailModal, e modalBackdrop, closeButton e buttonPressed não foram transformados em abstrações compartilhadas sem necessidade real.
* PetFormModal concentra o estado coeso e as operações exclusivas do cadastro, enquanto RegistrarPet mantém visibilidade, lista, carregamento, seleção e integração com ocorrência.
* TipoEspecie e FotoSelecionada permanecem internos a PetFormModal; nenhum tipo antecipado foi adicionado a pet.types.ts.
* expo-image-picker permanece diretamente no componente do domínio que possui o fluxo e sua abstração tecnológica continua reservada para a Fase 10 — Infrastructure.
* O coordenador da aba foi renomeado de RegistrarPet para PetsTab porque sua responsabilidade atual abrange toda a composição de Pets dentro de Profile.
* PetList, PetPhotoPicker e usePets não foram criados porque o código real não demonstrou responsabilidade adicional ou reuso concreto suficiente; a árvore do Plano Mestre permanece conceitual e progressiva.

======================================================================
PRÓXIMO PASSO
=============

Aguardar aprovação manual do smoke test da Fase 7P.

======================================================================
FASES
=====

[X] Fase 0  — Baseline / branch — CONCLUÍDA
[X] Fase 1  — Navigation — CONCLUÍDA
[X] Fase 2  — Estrutura modular base — CONCLUÍDA
[X] Fase 3  — Auth — CONCLUÍDA
[X] Fase 4  — Feed — CONCLUÍDA
[X] Fase 5  — Profile — CONCLUÍDA
[X] Fase 6  — Pets — CONCLUÍDA
[ ] Fase 7  — Occurrences — EM ANDAMENTO
[ ] Fase 8  — Map
[ ] Fase 9  — Care / Engagement / Reports quando aplicável
[ ] Fase 10 — Infrastructure
[ ] Fase 11 — Shared
[ ] Fase 12 — Theme
[ ] Fase 13 — App
[ ] Fase 14 — Types
[ ] Fase 15 — Tests
[ ] Fase 16 — Limpeza final

Última fase concluída:
Fase 6 — Pets.

Fase atual:
Fase 7 — Occurrences — EM ANDAMENTO.

Próxima fase:
Smoke test manual da Fase 7P — PENDENTE.

Subetapas concluídas:
* Extração dos tipos de navegação para `src/navigation/navigation.types.ts`.
* Extração do AuthNavigator para `src/navigation/AuthNavigator.tsx`.
* Extração do AppNavigator para `src/navigation/AppNavigator.tsx`.
* Extração do RootNavigator e da decisão autenticado/não autenticado para `src/navigation/RootNavigator.tsx`.
* Checkpoint arquitetural da estrutura modular base, sem criação de pastas vazias ou movimentação de arquivos.
* Fase 3A — movimentação de TipoCadastroScreen.tsx para `features/auth/screens/`.
* Fase 3B — movimentação de EsqueceuSenhaScreen.tsx para `features/auth/screens/`.
* Fase 3C — movimentação de LoginScreen.tsx para `features/auth/screens/`.
* Fase 3D — movimentação de CadastroUserScreen.tsx para `features/auth/screens/`.
* Fase 3E — movimentação de CadastroONGScreen.tsx para `features/auth/screens/`.
* Fase 3F — extração das cinco operações HTTP de Auth para `features/auth/services/authService.ts`.
* Fase 3G — fechamento formal da Fase 3 — Auth, sem alteração de código funcional.
* Fase 4A — movimentação de FeedBannerCarousel.tsx para `features/feed/components/`.
* Fase 4B — movimentação de FeedNoticias.tsx para `features/feed/screens/`.
* Fase 4C — extração de OccurrenceCard.tsx, feed.types.ts e feed.utils.ts dentro do domínio Feed.
* Fase 4D — extração de FeedControls.tsx e compartilhamento de FiltroFeed e ModoFeed em feed.types.ts.
* Fase 4E — movimentação de feedButtonPressedStyle para `features/feed/styles/feed.styles.ts` e avaliação documental da organização interna restante.
* Fase 4F — consolidação técnica das alterações validadas de 4D + 4E em um único checkpoint.
* Fase 4G — fechamento formal da Fase 4 — Feed, sem alteração de código funcional.
* Fase 5A — movimentação de ProfileQuickMenu.tsx para `features/profile/components/`.
* Fase 5B — movimentação de ProfileDetailScreen.tsx para `features/profile/components/`.
* Fase 5C — extração das quatro operações HTTP próprias de Profile para `features/profile/services/profileService.ts` e dos três tipos reutilizados para `features/profile/types/profile.types.ts`.
* Fase 5D — avaliação final da organização interna e fechamento formal da Fase 5 — Profile, sem alteração de código funcional.
* Fase 6A — movimentação estrutural de RegistrarPet.tsx para `features/pets/components/`, aprovada manualmente pelo usuário.
* Fase 6B — extração do contrato Pet para `features/pets/types/pet.types.ts` e das duas operações HTTP para `features/pets/services/petService.ts`, aprovada manualmente pelo usuário.
* Fase 6C — extração do componente visual PetCard para `features/pets/components/PetCard.tsx`, aprovada manualmente pelo usuário.
* Fase 6D — extração do modal visual PetDetailModal para `features/pets/components/PetDetailModal.tsx`, aprovada manualmente pelo usuário.
* Fase 6E — extração do formulário coeso PetFormModal para `features/pets/components/PetFormModal.tsx`, aprovada manualmente pelo usuário.
* Fase 6F — renomeação semântica de `RegistrarPet.tsx` para `PetsTab.tsx`, aprovada manualmente pelo usuário.
* Fase 6G — fechamento formal da Fase 6 — Pets, sem alteração de código-fonte.
* Fase 7A — movimentação estrutural de `OccurrenceDetailDrawer.tsx` para `features/occurrences/components/`, aprovada manualmente pelo usuário.
* Fase 7B — movimentação estrutural de `CadastroOcorrenciaScreen.tsx` para `features/occurrences/screens/`, aprovada manualmente pelo usuário.
* Fase 7C — extração do tipo-base compartilhado `OcorrenciaResumo` para `features/occurrences/types/occurrence.types.ts`, aprovada manualmente pelo usuário.
* Fase 7D — centralização das três operações compartilhadas de leitura em `features/occurrences/services/occurrenceService.ts`, aprovada manualmente pelo usuário.
* Fase 7E — centralização das operações nucleares de criação, atualização e exclusão em `features/occurrences/services/occurrenceService.ts`, aprovada manualmente pelo usuário.
* Fase 7F — extração do corpo interativo da seção de tipo de ocorrência para `features/occurrences/components/form/OccurrenceTypeSection.tsx`, aprovada manualmente pelo usuário.
* Fase 7G — extração do grid visual de tipo do animal para `features/occurrences/components/form/AnimalTypeSelector.tsx`, aprovada manualmente pelo usuário.
* Fase 7H — extração do componente reutilizável ChoiceButton para `features/occurrences/components/form/ChoiceButton.tsx`, aprovada manualmente pelo usuário.
* Fase 7I — extração do elemento visual/interativo SelectionChip para `features/occurrences/components/form/SelectionChip.tsx`, aprovada manualmente pelo usuário.
* Fase 7J — extração do componente de grupo SelectionChipGroup para `features/occurrences/components/form/SelectionChipGroup.tsx`, aprovada manualmente pelo usuário.
* Fase 7K — centralização dos seis styles compartilhados do formulário em `features/occurrences/components/form/occurrenceForm.styles.ts`, aprovada manualmente pelo usuário.
* Fase 7L — extração do shell visual compartilhado das nove seções para `features/occurrences/components/form/OccurrenceFormSection.tsx`, aprovada manualmente pelo usuário.
* Fase 7M — extração do conteúdo controlado da seção Sobre o animal para `features/occurrences/components/form/AnimalSection.tsx`, aprovada manualmente pelo usuário.
* Fase 7N — extração da subseção controlada Saúde crítica para `features/occurrences/components/form/HealthSection.tsx` e centralização dos styles condicionais compartilhados, aprovada manualmente pelo usuário.
* Fase 7O — extração da subseção controlada Deficiência para `features/occurrences/components/form/DisabilitySection.tsx`, aprovada manualmente pelo usuário.
* Fase 7P — extração do conteúdo controlado da seção Cuidados iniciais para `features/occurrences/components/form/InitialCareSection.tsx`, com validação técnica concluída e smoke test manual pendente.

Último arquivo alterado pela refatoração:
`mobile/src/features/occurrences/components/form/InitialCareSection.tsx` e `mobile/src/features/occurrences/screens/CadastroOcorrenciaScreen.tsx`.

Arquivo de acompanhamento criado:
STATUS_REFATORACAO_PETRADAR.txt

Último arquivo alterado nesta etapa documental:
STATUS_REFATORACAO_PETRADAR.txt

Próximo passo:
Aguardar aprovação manual do smoke test da Fase 7P; não iniciar nova subetapa automaticamente.

Pendências futuras de limpeza observadas no App.tsx e preservadas nesta tarefa:
* AppStack
* createNativeStackNavigator
* ActivityIndicator
* ProfileDetailScreen

======================================================================
